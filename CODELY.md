

## Codely Structured Memories

### User

### Feedback
- [2026-08-07 07:31:59] combat composable 不应直接 import useTalentStore()/useCharacterStore() 等 Pinia store。应通过 ICombatContext 接口访问（ctx.talent.xxx / ctx.character.xxx），测试 mock 时在 ctx 中提供占位值即可。直接 import store 会导致测试中 Pinia 未初始化报 "Cannot read properties of undefined (reading '_s')"。createCombatContext 是 combat 模块唯一 import 外部 Store 的位置。
- [2026-08-07 08:43:37] vi.hoisted 回调在模块 import 之前执行，不能在 vi.hoisted 内部使用 `ref()` 等 Vue API（报 ReferenceError: Cannot access before initialization）。**Why:** vi.hoisted 设计目的是在 mock 工厂中引用 stub 对象，保证 hoisting 顺序。**How to apply:** gameStore mock 的 currentCharacterId 用普通对象属性即可（exploration 测试模式）；仅在需要在测试执行中途改变 currentCharacterId 且 computed 需要响应式追踪时（如 equipment 的并发竞态测试），用 getter/setter 模式替代 ref。
- [2026-08-07 16:28:05] 用户偏好：修复中遇到魔法数字（比例、系数等）时应抽取到 src/config/ 对应配置文件中作为命名常量，而非直接内联。**Why:** 用户在 P3-150 修复时主动要求将死亡经验保留比例抽取到 config/character.ts。**How to apply:** 修改 service/store 中含硬编码数值（如 0.5、0.7 等）的公式时，主动将数值提取为配置常量。
- [2026-08-11 08:50:43] Vue3 SFC 组件如果模板有多个根节点（如主内容 + EmptyState + ConfirmPopup 并列），`v-show` 只对第一个根节点设 `display: none`，其余节点仍可见。**Why:** v-show 在多根节点组件上只操作第一个根元素的 style。**How to apply:** 被 v-show 控制的面板组件必须用单一 `<div>` 包裹所有内容作为唯一根节点，否则 tab 切换时内容不会正确隐藏。
- [2026-08-11 19:34:00] vue-virtual-scroller DynamicScroller 间距必须加在 `.vue-recycle-scroller__item-view > div`（DynamicScrollerItem 的根 div）上，不能加在 `.vue-recycle-scroller__item-view` 上。**Why:** DynamicScroller 测量 item-view 的直接子元素 offsetHeight 来定位，padding 在 item-view 上不会被计入尺寸，导致条目重叠、间距消失。**How to apply:** 任何使用 DynamicScroller 的组件需要条目间距时，padding/margin 加在子元素而非 item-view。RecycleScroller gridItems 模式则相反——padding 加在 item-view 上（因为 RecycleScroller 用 itemSize 直接定位，不测量子元素）。

### Project
- [2026-08-06 11:52:49] wow_dnd 是 Vue3+TS+Vite+Dexie(IndexedDB) 的Web单机游戏「战争艺术：地下城」，非 Unity。数据分层：src/data/config_*.ts 静态常量(启动时 bulkPut 导入 IndexedDB)→27张表(config_*/char_*/runtime_*)→各模块 db.ts→Pinia Store。A层设计不入DB：config_mounts(程序化生成)与config_area_events(含函数不可序列化)。
- [2026-08-06 11:52:55] 2026-08-06 清除配置数据双源歧义：新建 src/modules/config/cache.ts (ConfigCache, 懒加载+Promise去重) 统一从DB读天赋树/被动/套装定义；ATTRIBUTE_POTION_IDS白名单改为 capabilityTypes 的 attribute_potion 标签；quest/objective_utils 敌名IIFE 改 initEnemyNameMap() 从DB加载；GameBootstrap 新增 Layer0 预加载。破坏性更新，不兼容旧存档。
- [2026-08-06 17:26:53] 2026-08-06 战斗公式统一重构完成：(1) 移除荆棘反伤机制（thorn handler/pipeline Stage 4/computeThornsDamage/6个composable荆棘代码块/数据配置thorn buff全部清除）。(2) pipeline防御拆分：calcBaseDamage→calcAttackDamage+applyDefenseReduction，防御始终生效（含技能），减伤公式改 max(floor(伤害×系数), 防御) 防御全额生效。(3) 新增配置常量 PHYSICAL_DEFENSE_REDUCTION_COEFFICIENT/MAGICAL_DEFENSE_REDUCTION_COEFFICIENT/HEAL_BONUS_DIVISOR，删除 DEFENSE_REDUCTION_COEFFICIENT。(4) UI显示修正：getSkillEffectText/getSkillEffectBrief 接受Stats参数展示含主属性加成预估值。(5) 治疗接入healBonus+暴击：castSkill不再直接receiveHeal，由usePlayerSkill统一应用 floor(治疗×(1+healBonus/100)×暴击倍率)。(6) 删除hpBonus死代码（HP_BONUS_CON_COEFFICIENT/calculateHpBonus/Attributes.hpBonus）。
- [2026-08-07 07:31:56] 2026-08-06 天赋系统「三系六层」网状改造全部完成（plan.md 四阶段）。阶段一：TALENT_POINT_RULES 重写（10级起每级2点，20级22点，rowUnlockRequirements=[0,3,6,9,12,15]，maxPointsPerTalent=2）；Talent 新增 col 字段；暴击率改由职业主属性推导（calculateCritChance 加 primaryStat 参数），evoker int→cha；效果消耗全部接入（stat_bonus/damage_multiplier/damage_reduction/resource_bonus/skill_enhance）；天赋分配持久化到 Character.talentAllocations；pet 新增 lockPet 回退。阶段二：isTierUnlocked 改全树累计。阶段三：13职业×18节点完整重写（3树合并为1棵6×3网状树，crit_bonus→stat_bonus，hp_multiplier→stat_bonus(con)，mana_max→stat_bonus(int)，质量修复）。阶段四：TalentPopup.vue 网状UI + console 命令 + validate。关键架构：ICombatContext 新增 talent 域（getter 代理 useTalentStore），combat composable 不直接 import useTalentStore，保持测试隔离。tsc/eslint 零错误，630 项天赋+战斗测试全通过。
- [2026-08-11 08:50:43] - 预存测试失败：全量 `vitest run` 有 ~51 项失败，均为预存问题——25 个组件测试文件中 `wrapper.emitted()` 返回 undefined（Vue Test Utils 版本兼容问题）+ `test/config/version.test.ts` 报 `No such built-in module: node:`。GameMain.test.ts 中 `wrapper.vm.showCharacterInfo` 未通过 `defineExpose` 暴露也属预存。模块测试（排除组件+version.test 后）全通过。



- [2026-08-07 13:30:04] P3-167 怪物/Boss 扩展完成（2026-08-07）：新增 20 个普通怪物（Lv1-5 ×2, Lv6-10 ×4, Lv11-15 ×6, Lv16-20 ×8）+ 10 个 Boss（Lv1-5 ×2, Lv6-10 ×3, Lv11-15 ×3, Lv16-20 ×2）+ 9 个怪物技能 + DROPS_DEADLY 掉落常量。46 个地点的 enemies/bosses 数组全部更新。怪物总计 48、Boss 总计 16。67 项新测试全通过。
- [2026-08-07 16:13:43] P3-162 AI 决策类型扩展完成（2026-08-07）：AiDecision 新增 buff/defend（3→5种）。BattleContext 新增 enemyHasBuff/playerHasDebuff 字段（useEnemyAction 用 hasEffect 填充）。4 种策略重写：决策维度从仅敌人HP扩展为 敌人HP+玩家HP+回合数+已有buff状态。技能三分类（heal/buff/attack 替代原 heal/non-heal）。新增 4 个怪物技能（war_cry/fear_roar/stone_skin/weaken）+ 10 个 Boss skillPool 追加。defend 非技能行为不消耗冷却，施加 defense_up+10 持续 2 回合。CombatActionType 加 'defend'，CombatEventType 加 'combat_defend'。25 项新测试全通过。
- [2026-08-07 17:50:58] P3-149+P3-136 任务类型扩展与内容补齐完成（2026-08-07）：QuestType 从 kill|collect 扩展为 kill|collect|explore（用户决策去掉 talk）。checkQuestProgress 新增 explore 匹配（explored=true 触发，locationId 可选过滤区域）。store.ts 新增 onCellExplored(locationId?) action。exploration/store.ts 在 revealGrid 路径3(treasure/trap/event/rest) isFirstVisit 和 onBattleResult 胜利/失败分支 !cell.explored 处调用 questStore.onCellExplored(currentAreaId)。exploration store 新增 import useQuestStore（无循环依赖：quest 不 import exploration）。objective_utils getObjectiveText 新增 explore 文本（无 locationId→"探索{N}格区域"，有→"探索{locationId}区域"）。config_quests.ts 追加 20 个任务（10 collect + 10 explore），collect 用 LOOT_ITEMS 中已有 itemId（magic_dust/small_health_potion/bread/roasted_meat/dragon_scale/primal_earth/primal_water/void_crystal 等），explore target 8-20 格部分限定 locationId。UI 图标：explore=compass(nature)。442 项测试全通过。
- [2026-08-10 09:24:25] 2026-08-10 第二轮全量代码审查修复完成（P5-001~031）：5 子代理并行覆盖 src/ 全量源码，发现 31 项问题。P1：mana_restore 不结束回合（useSkillCasting 设置 heal=damageValue 复用 heal 分支，跳过 receiveHeal）、checkCompletion OR→AND、多选项事件自动选第一项（新建 MultiOptionEventPopup.vue 弹窗组件）、移动端触摸点击失效（onTouchEnd 补点击判定）、属性药剂永久叠加（回退为设计保留）。P2：治疗暴击 multiplier、Boss healing_zone 零值 ?? 、createCharacter 种族校验、sellItem 事务回滚、天赋 maxPointsPerTalent 生效、enterZone 等级校验、事件 completed 时机、debuff 显示方向、useGameActions disposed 竞态、ExplorationView try-catch、validate 交叉引用。P3：非空断言守卫、EffectContext 透传、TDZ 声明前移、as PetType、Object.freeze、popupMounted 复位、Map 拖拽边界钳制、eslint 19 warnings 清零。3 项回退为设计保留（P5-005 applyBonus 路径、P5-008 changeMp 两次、P5-028 grid gap 公式）。tsc/eslint/vitest 97文件3531项全通过。归档 fixed_20260810092323.md。
- [2026-08-10 10:17:40] 2026-08-10 第三轮全量代码审查全部修复完成（P6-001~204）：5 子代理并行覆盖 src/ 全量 302 源码，发现 42 项问题。修复 30 项，验证排除 6 项，设计保留 6 项。P1：P6-200 备份/导入遗漏 5 个配置表。P2：P6-001 多敌战斗误判胜利、P6-003 混合 buff+debuff 丢弃、P6-050 消耗品效果静默忽略、P6-051 sellItem soldItems 未回滚、P6-052 capabilities 崩溃兜底、P6-053 套装重算无 try-catch、P6-101 失败/逃跑触发探索进度、P6-102 empty 概率负值、P6-201 LogStore 角色ID。P3：P6-004 Boss 魔法数字抽 config、P6-005 additive/independent 文档标注、P6-006 applyPhaseStats !== undefined、P6-007 返回值 actualDamage、P6-009 Promise.resolve().catch()、P6-054 initialize 重置 lastAppliedStats、P6-055 限购部分购买、P6-057 effectiveStats 上限判定、P6-100 revealAllCells 补 bossDefeated、P6-105 areaId=location.id、P6-106 安全离开不回血、P6-107 getState Object.freeze、P6-152 cleanup 幂等、P6-153 回退对象完整字段、P6-154 移除废弃 wrapAsync、P6-155 音频重试监听、P6-202 ConfigCache 失败复位、P6-204 getExpForLevel 返回 Infinity。tsc/eslint 零错误，173文件5338项全通过。归档 fixed_20260810100500.md + fixed_20260810101500.md。current.md 待办归零。
- [2026-08-10 10:36:52] 2026-08-10 第四轮全量代码审查完成（P7-001~033）：6 子代理并行覆盖 302 源文件，主代理逐项验证后写入 doc/fixed/current.md。P1×1（P7-001 runtime_shopSoldItems 表未纳入备份/导入管道）、P2×5（onEnemyKilled 未 await、unequipItem 缺 try-catch、MigrationService 忽略 importData 失败、useShopTransactions 缺 try/catch、BaseIcon gradient 切换不 re-render）、P3×27。排除 2 项误报：P7-EX-01 子代理报告 Quest 缺 daily/elite/chain（Unity 记忆不适用于 Vue3 项目）、P7-EX-02 computeEventProbability 舍入偏差（P6-102 已修复）。覆盖缺口：子代理预算耗尽，combat/effects/resources(16)/pets(6)/forms(5)、talents(4)、src/data(20)、大量 popup/common 组件未完整读取，已在 current.md 末尾标注。
- [2026-08-10 11:36:58] 2026-08-10 第四轮全量代码审查修复完成（P7-001~033）：6 子代理并行覆盖 302 源文件，发现 33 项问题（P1×1 P2×5 P3×27），主代理逐项验证后全部修复。P1：runtime_shopSoldItems 表补入 BackupData/collectAllData/importData 管道。P2：onEnemyKilled 改 Promise.all+catch、unequipItem reapplySetBonuses 加 try-catch、MigrationService 校验 importData 返回值、useShopTransactions handleBuy/handleSell 加 try/catch、BaseIcon gradient 缓存键改 icon:grad。P3：INITIATIVE_DEFAULT_SPEED 提 config、saveLogs forceAll 不复用旧 promise、applySkillBuffs 移除 self 限制、computeResurrection 0.5 提 config、selectCharacter 过期注释修正、equipItem applyBonus catch 补 flushPersist、revealGrid shop/board 死代码删除、checkCompletion 放宽为 bossDefeated+visitedCells>=BOSS_SEAL_REQUIRED_CELLS、db.ts playerPosition 越界 clamp、enemy dex 乘 levelScale、checkQuestProgress 补建缺失 progress 条目、_grantQuestRewards console.warn 改 errorReporter、applyPhaseStats 基于 originalBaseStats 重算防累积连乘、console/commands/quest.ts 缓存 store 引用、admin/store saveRecord id 运行时校验、applyVolume 用 effectiveSfxVolume/effectiveBgmVolume、playSfx/setBgmScene tryResume 后重放、runMigrations 改链式步进、useCharacterCreation doCreate 运行时校验+selectRace 清除 class、useConfigCrud try/catch+id 校验、MapView 魔法数字提 config/map.ts、InventoryPopup 缓存 store 引用、useGameActions handleBattleTriggered try/catch。3 个测试文件更新匹配新行为。tsc/eslint 零错误，vitest 5776 项通过（50 项预存组件测试失败）。归档 fixed_20260810113526.md。
- [2026-08-10 12:25:14] 2026-08-10 第五轮全量代码审查修复完成（P8-001~512）：11 子代理分两波并行覆盖 302 源文件，发现 62 项问题（P2×4 P3×58），修复 59 项，验证排除 3 项（P8-004 AI isHeal/isBuff 互斥正确；P8-018 adventureLog 主键 characterId 覆盖正确；P8-019 withRetry 末尾 throw 不可达但满足 TS 返回类型推断）。P2：P8-001 endCombat catch 兜底 emit COMBAT_END；P8-007 mana_restore heal 分支改用 result.heal+healType:'mana'+跳过暴击+CombatPopup doSkill 蓝字 MP+；P8-101 on_damaged 治疗被动改用 maxHp 百分比（仅 on_attack 走吸血分支，传 trigger 参数到 applyHeal）；P8-501 CombatPopup useItem 补 result.success 检查防卡死。P3 重点：store.ts attack/skill 加 !combatResult 守卫；calculateFleeChance 钳制 [0,1]；combatContext 单次 useTalentStore；ResourceSystemFactory 加 _cache Map；unlearn 加行解锁降级校验；createCharacter 加存在性校验；双手武器换装先卸 weapon2；migrations 空注册表返回/非空缺口 throw；initializer initPromise 互斥锁；useCamp 标记真实营地格；audio pendingSfxTypes 去重；boss damage_shield 0 清空护盾；phaseManager maxHp<=0 短路；BaseIcon loadToken 竞态保护；10 个组件 onMounted/handler 加 try/catch+toast；TalentPopup setTimeout 清理+learn/unlearn 返回值检查；MapView zoom 后 clampPan；ResourceBar percent 钳制；console kill await+CONSOLE_KILL_DAMAGE 常量+aliveEnemies 检查；mount 命令 STAT/TIER 映射去重用 config。10 个测试更新匹配新行为。tsc/eslint 零错误，vitest 5776 项通过（50 项预存组件测试失败）。归档 fixed_20260810122430.md。
- [2026-08-10 13:38:17] 2026-08-10 第六轮全量代码审查修复完成（P9-001~109）：7 子代理并行覆盖 303 源文件，发现 109 项问题（P1×7 P2×37 P3×65），修复 27 项（P1×7 P2×20），验证排除 1 项（P9-020 combatLogs 无 characterId 索引）。P1 全部修复：BaseResourceSystem.reset() 新增 baseMaxValue 重置 maxValue 防跨战斗累积；AOE/宠物/DOT 三处击杀 Boss 补 checkBossRevive；宠物联动伤害补 applyBossDefenseMechanics/applyBossCounterMechanics；playerSkill 末尾兜底 endPlayerTurn 防卡死；equipment/talent initialize 改对齐基线（syncAppliedBonuses + lastAppliedStats={...statBonuses}）跳过 applyBonus 防重复施加。P2 重点：日志截断方向 slice(0,N) 保留最新；itemTemplate.maxStack 替代硬编码 MAX_STACK；FocusSystem 补 onDamaged；summon_pet 按 currentOwner 查资源类型；ConfigCache.invalidate 清 loadingPromise 防竞态；备份 50MB 大小限制；版本降级 throw；onLevelUp 调 talentStore.updateLevel+skillStore.checkLevelUnlocks；syncAllocationsToCharacter 补 persistCharacter（character store 导出 persistCharacter）；IIFE 改同步防竞态；equipment initialize try/finally；Boss phases 深拷贝；lastTriggerTurn !==undefined 替代 falsy；CharacterSelectPopup 去 selectCharacter 仅 emit；Tag customStyle 改 computed；BossPhaseStrategy 合并冗余分支+PLAYER_LOW_HP_THRESHOLD。tsc/eslint 零错误，vitest 5775 项通过（51 项预存失败）。归档 fixed_20260810134000.md。
- [2026-08-10 14:19:19] 2026-08-10 第七轮代码审查 P2 修复完成（16/16 项）：P9-010 Boss 反击改注入 onDamage 回调，宠物攻击反击施加给宠物；P9-014 dropItemsByIndices 补日志；P9-015 onItemCollected 回调包装 catch；P9-017 SET_PARTS 合并到 initEquipment 持久化到 DB；P9-018 importData 改 clear+bulkPut 防旧数据残留；P9-023 castSkill 传完整 CanCastSkillOptions（含 currentCooldown）；P9-029 equipItem catch 补回滚 previousEquipped；P9-030 套装 bonus key 加 requiredPieces 防碰撞（遍历 activeTiers 替代 getActiveBonusEffects）；P9-033 routeSynthTo 断开共享效果器到各通道再重连防 fan-out 音量倍增；P9-034 movePlayer 穿 completed 格补 refreshGrid；P9-035 createEnemy 返回缓存引用；P9-036 applyEventChoice 用 pendingEventCell 精确定位事件格；P9-039 所有 addEffectToContainer 调用方传入 registry；P9-040 removeEffectFromContainer 加 registry+ctx 参数调 onRemove；P9-041 attackMultiplier 移到防御减免前应用；P9-043 endCombat await handleDeath。测试更新：data/service.test.ts mock 补 SET_PARTS:[]、skill/store.test.ts canCastSkill 断言改 objectContaining。tsc/eslint 零错误，vitest 51 项预存失败无新回归。
- [2026-08-10 15:27:40] 2026-08-10 第七轮代码审查 P2+P3 全部修复完成（81/81 项）：P2×16（P9-010~043）+ P3×65（P9-042~109）。P2 重点：Boss 反击注入 onDamage 回调（宠物攻击反击施加给宠物）、SET_PARTS 合并到 initEquipment、importData 改 clear+bulkPut、canCastSkill 传完整 CanCastSkillOptions、套装 bonus key 加 requiredPieces、routeSynthTo 断开共享效果器防 fan-out、attackMultiplier 移到防御减免前、await handleDeath。P3 重点：usePetAction 改参数注入 petStore、forms/store.ts 改 setFormContext 注入（GameBootstrap 接线）、hp_multiplier 死代码清除、enemy 治疗改 maxHp*0.15、shield 耗尽标记 remainingTurns=0、12 个 Vue 组件加 try/catch+toast、AI 策略 injectAiRng、deepFreeze 递归冻结、importBackup 单次读取。P9-090 验证排除（organVoice BGM 独立路由，设计如此）。8 个测试文件更新匹配新行为。tsc/eslint 零错误，vitest 5779 项通过（51 项预存失败无新回归）。归档 fixed_20260810152700.md，current.md 待办归零。
- [2026-08-10 16:08:14] 2026-08-10 第八轮全量代码审查进行中（P10-001~030+201）：8 子代理覆盖 303 源文件，发现 33 项问题（P1×3 P2×7 P3×23）。P1 修复完成：P10-001 usePlayerSkill 单体击杀误判胜利（与 playerAttack P6-001 对齐：先 checkBossRevive 再判 aliveEnemies.length===0）、P10-002 char_quests 备份改数组形状（BackupData.quests 从 Record 改为 CharQuestStorage[]，importData 改 bulkPutArrayIfNotEmpty）、P10-201 routeSynthTo 重连到分类通道而非 masterVolume（P9-033 引入的回退，效果器→分类通道→masterVolume）。P2 修复完成：P10-003 weapon2 回滚暂存 unequippedWeapon2、P10-004 canUnlearn 加 meetsRequirements 校验、P10-005 任务过期条目 filter、P10-006 handleEventChoice try/catch+finally 关弹窗、P10-007 handleCellExplored try/catch、P10-A App.vue refreshData await+try/catch。P3 由 3 个子代理并行修复中。
- [2026-08-10 17:12:25] 2026-08-10 第八轮全量代码审查修复完成（P10-001~049）：8 子代理并行覆盖 303 源文件，发现 49 项问题（P1×2 P2×9 P3×38），全部修复。P1：P10-001 usePlayerSkill 单体击杀误判胜利（与 playerAttack P6-001 对齐）、P10-005 char_quests 备份折叠数据丢失（toCharacterRecord 按 characterId 折叠导致同角色多任务互相覆盖，改为数组形状存储）。P2：clearContainer 不触发 onRemove、双手武器回滚缺 weapon2、天赋 canUnlearn 不校验前置依赖、任务进度过期条目永久阻塞、routeSynthTo 战斗路由未连 fmSynth/metalSynth/synth、AdminTable format 缺 row、BaseDbService CRUD 无 try-catch、宠物 skillCooldowns Map 不可序列化、pipeline applyEffect 未透传 ctx。P3 重点：P10-039 统一 attack_up/attack_down 与 passive multiplier 在减伤前合并应用（原 effect 系在减伤后）、魔法数字提取 config（FREEZE_SPEED_MOD/TALENT_NODE_W/talentLayout.ts）、AdminForm as 断言改 instanceof、ShopPopup 卸载守卫、useShopTransactions 下界钳制、createParticleBurst 增加 cancel() 控制器、GameBootstrap.dispose 补 clearPetCallbacks、getHpPercent 除零守卫、getQuestRequiredMonsters 去重。ResourceSystemFactory 新增 clearCache() 方法。3 个测试文件更新匹配 P10-039 管线变更+P10-031 音频路由变更。tsc/eslint 零错误，vitest 5777 项通过（53 项预存失败）。归档 fixed_20260810161000.md。current.md 待办归零。
- [2026-08-10 18:19:10] 2026-08-10 UI 菜单合并 Phase 1-2 完成：底部导航 7→5（角色|背包|构筑|进度|系统）。技能+天赋合并为 BuildPopup（分段标签），任务+日志合并为 ProgressPopup（分段标签）。新建 src/components/popup/panels/ 目录提取 SkillsPanel/TalentsPanel/QuestPanel/LogPanel 内容组件（无 BasePopup 外壳），原弹窗文件保留不删（测试兼容）。新建 MenuBadge.vue 角标组件，5 个入口均接徽章（角色=未分配属性点、背包=满度≥90%、构筑=未分配天赋点、进度=任务数+日志未读）。Store 新增 questStore.activeCount、logStore.unreadCount+markAllRead。useGameActions 合并 showSkills/showTalents→showBuild、showQuests/showAdventureLog→showProgress。关键决策：不拆分移动端/PC 渲染层，统一 markup+分段化降密度+菜单承载状态。tsc/eslint 零错误，vitest 5778 通过/52 预存失败无新回归。
- [2026-08-10 18:19:10] vue-tsc@3.3.8 + typescript@5.3.2 存在兼容性问题：`vue-tsc --noEmit` 报 `ReferenceError: isSatisfiesExpression is not defined`（@vue/language-core 的 isSatisfiesExpression 未定义）。**Why:** vue-tsc 版本与 TypeScript 版本不匹配。**How to apply:** 用 `npx tsc --noEmit --skipLibCheck` 代替 vue-tsc 做类型检查（.vue 文件的 script 部分仍可被 tsc 检查），vue-tsc 仅在 `pnpm build` 时使用（build 会成功因为 vite 不依赖 vue-tsc 的类型检查阶段）。此为预存问题非本次引入。
- [2026-08-11 08:08:53] 2026-08-11 UI Phase 3 完成：CharacterInfoPopup 分段化（1552→~100行容器）+ 装备流收敛。新建 popup/panels/ 目录下 4 个面板子组件：CharacterOverview（概览+次级属性，删除与 HUD 重复的资源条）、CharacterAttributes（六维属性+分配+7层来源tooltip）、CharacterEquipment（只读+卸下+"前往背包"跳转，装备主入口收敛到 InventoryPopup）、CharacterMounts（坐骑5档）。CharacterInfoPopup 改为 [概览][属性][装备][坐骑] 分段标签容器，有未分配点数时默认进入属性段。GameMain 接收 @open-inventory 事件实现角色→背包跨弹窗跳转。tsc/eslint 零错误，vitest 5778 通过/52 预存失败无新回归。
- [2026-08-11 19:25:32] 2026-08-11 图标系统重构完成（两轮）：(1) 新建 src/config/icons.ts 集中管理系统级公共图标常量（FALLBACK_ICON/NAV_ICONS/CELL_ICONS/EFFECT_ICONS/QUEST_ICONS/STAT_ICONS/RESOURCE_ICONS/SLOT_ICONS/COMMON_ICONS）。(2) BaseIcon.vue 修复回退逻辑：始终调 loadIcon 校验存在性，不存在时 effectiveIcon 回退为 FALLBACK_ICON（渐变+单色模式均生效）。(3) 从 iconify API 拉取 game-icons 全集（4134 个有效名）校验，第一轮修复 22 个源文件中 128 处无效图标名（技能 config_skills.ts 36处、天赋 config_class_talents.ts 37处问题最多）。(4) 第二轮修复 37 处语义错误的天赋图标（如 avoidance→fluffy-swirl 暗影之舞、double-quaver→duality 双武器、windsock→fog 复苏之雾、broken-heart→shatter 湮没），减少 lightning-storm 从 13 处降至 1 处等泛用图标重复。(5) config/icons.ts 已全面接入 25+ 组件/composable。4 个测试文件同步更新。tsc/eslint 零错误，vitest 5777 通过/52 预存失败无新回归。 - [2026-08-11 18:47:00] 2026-08-11 探索场景等距 2.5D 升级完成（经历多轮迭代修正）。最终方案：等距坐标系绝对定位 + clip-path:path() SVG 路径绘制菱形。改动 2 文件：(1) config/exploration.ts 新增 ISO_TILE_W=52/ISO_TILE_H=26 + AreaTheme 类型 + AREA_THEME_MAP（49 区域→10 主题，tile-surface 用 rgba(...,0.6) 透明）+ DEFAULT_AREA_THEME。(2) ExplorationView.vue：格子按 isometric 投影 absolute 定位（left=(x-y+gridSize-1)*tileW/2, top=(x+y)*tileH/2）；cell-surface 全尺寸菱形（clip-path:path()），::before 同尺寸菱形做填充背景（无边框线）；墙只画 wall-top + wall-left 两面（利用冗余存储避免重叠），2px 厚 + drop-shadow 发光；player-here 用 ::after + evenodd path 描边；accessible/movable 格用 .cell-surface 绿色背景（不呼吸）区分于 danger 的红色呼吸；movable+danger 同时存在时用多层 background 叠加（红色径向渐变 + 绿色底色 + danger-breathe 动画）；accessible 格无图标；iso-grid::after 下沉模拟厚度。缩放：PC 滚轮（0.3~2.5），移动端双指 pinch。grid-wrapper aspect-ratio:1 正方形。关键教训：①等距渲染不能用 skewX 或容器整体 rotateZ，必须用 isometric 坐标投影+逐格 clip-path；②clip-path 裁掉 box-shadow，描边必须用 path evenodd；③linear-gradient 角度无法匹配菱形边缘，必须用 clip-path:path()；④**墙方向映射**：等距投影中 x→右上 y→左下，walls.top(dy=-1)→菱形右上边(Top→Right)，walls.left(dx=-1)→菱形左上边(Left→Top)；⑤墙数据四方向冗余存储，渲染只画 top+left 避免重叠；⑥父容器 background 被子元素（cell-surface/::before）完全覆盖，改背景必须改 .cell-surface。 - [2026-08-11 19:25:00] 2026-08-11 探索固定事件放置策略调整：placeFixedEvents 改为：起点→随机边缘；任务板+营地→起点相邻空位（getFourNeighbors 打乱后取前两个）；商店→四个角落中与起点切比雪夫距离最大的角落；Boss→中心区域。删除不再使用的 findNonAdjacentPosition 和 isAdjacent 函数。tsc/eslint 零错误，247 项探索测试全通过。







### Reference
- [2026-08-07 08:43:34] 项目规则文件位于 `.trae/rules/`：fix_rule.md（bug修复流程）、code_rule.md（编码与测试规范）、git-commit-message.md（全中文提交信息）、design_rule.md（文档编写规则）。用户说"按照规则"时指这些文件。fix_rule.md 要求：修复后移出 current.md 对应条目 → 创建 fixed_yyyyMMddHHmmss.md 归档 → 运行 tsc+eslint+vitest 验证。
