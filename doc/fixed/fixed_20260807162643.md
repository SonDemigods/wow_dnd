# P3-150 修复归档：死亡经验惩罚从全清改为损失 50%

> 修复时间：2026-08-07 16:26
> tsc：通过（0 错误）
> eslint：0 错误（5 预存 warning）
> vitest：216 项全通过

---

## 问题描述

**P3-150（玩法·P1）：死亡惩罚过严，无保险机制**

`computeResurrection` 死亡后 `exp: 0`（本级经验全部清零）+ 半血半蓝复活。19 级 999 经验时死亡损失接近一整级刷怪成果，单机游戏"劝退级"惩罚。

## 修复方案

仅执行方案一：经验惩罚从全清改为保留 50%（损失 50%）。

## 改动清单

### 1. `src/config/character.ts`
- 新增 `DEATH_EXP_RETENTION_RATIO = 0.5` 常量，抽取死亡经验保留比例到配置。

### 2. `src/modules/character/service.ts`
- `computeResurrection`：`exp: 0` → `exp: Math.floor(character.exp * DEATH_EXP_RETENTION_RATIO)`
- import 新增 `DEATH_EXP_RETENTION_RATIO`
- JSDoc 注释更新

### 3. `src/modules/character/store.ts`
- P2-57 设计说明注释："经验清零" → "损失部分本级经验"

### 4. `test/character/service.test.ts`
- 用例 "经验值清空" → "损失50%本级经验（保留半数）"，断言 `toBe(499)`
- 新增用例 "exp 为 0 时结果仍为 0"

### 5. `test/character/store.test.ts`
- mock `computeResurrection`：`exp: 0` → `exp: Math.floor(char.exp * 0.5)`
- 用例标题 "清空本级经验" → "损失50%本级经验"
- 断言 `toBe(0)` → `toBe(40)`（Math.floor(80 * 0.5)）

## 关联

- P2-57 设计保留项同步更新："经验清零" → "损失50%本级经验"
