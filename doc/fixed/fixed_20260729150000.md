# P2 问题修复归档

> 修复时间：2026-07-29
> 修复批次：P2 一般问题（功能/性能/规范）
> 测试状态：145 文件 / 3906 测试通过
> 涉及问题：48 项 P2 已修复，2 项延后/保留（P2-38 延后、P2-57 有意设计）

---

## 已修复问题

### P2-32. useBossMechanics catch 块仅有 console.warn，不符合异常处理规范
- **涉及文件**：[useBossMechanics.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useBossMechanics.ts#L195-L210)
- **修复方案**：catch 中进行优雅降级 —— 清理已创建的小怪避免半成品状态污染战斗，通过战斗日志记录失败原因，并使用 console.error 上报错误（替代原仅 console.warn 的吞异常行为）。

### P2-33. combat/db.ts 使用双重类型断言绕过类型检查
- **涉及文件**：[combat/db.ts](file:///d:/openSource/wow_dnd/src/modules/combat/db.ts#L63-L85)
- **修复方案**：移除 `as unknown as CombatLogStorage[]` 双重断言。Dexie 表已通过 schema 类型推断为 `CombatLogStorage[]`，直接使用 `const logs: CombatLogStorage[] = await ...` 即可，未来表类型变化由 TypeScript 编译期检查。

### P2-34. usePlayerAction 中 BossRuntime 类型断言访问 EnemyInstance 上不存在的字段
- **涉及文件**：[enemy/types.ts](file:///d:/openSource/wow_dnd/src/modules/enemy/types.ts#L299-L311)、[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L136-L138)
- **修复方案**：在 `EnemyInstance` 接口中显式声明所有 Boss 引擎运行时注入字段（`invulnerable?`、`shield?`、`reflectDamage?`、`counterStance?`、`canRevive?`、`charging?`），移除 `BossRuntime` 类型断言，直接通过 `target.invulnerable` 等访问。

### P2-35. combat/store.ts 中 initiativeRef 延迟绑定 hack
- **涉及文件**：[combat/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L46)
- **修复方案**：将 `let initiativeRef: ... | null = null` 替换为 `initiativeHolder: { current: ... | null } = { current: null }` 容器对象，避免 `let + null` 延迟赋值的反模式，语义更清晰。

### P2-36. combat/store.ts 中 endCombat 引用了在其后定义的 player 变量（TDZ 风险）
- **涉及文件**：[combat/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L149)
- **修复方案**：引入 `playerHolder: { current: ... | null } = { current: null }` 容器对象延迟绑定，`player` 初始化后赋值到 `playerHolder.current`，`endCombat` 内通过 `playerHolder.current?.handleLoot(e)` 调用，规避 TDZ 风险。

### P2-37. useInitiative 中 DOT 杀敌后重复调用 saveLogs
- **涉及文件**：[useInitiative.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useInitiative.ts#L239-L250)
- **修复方案**：移除 `tickAllEffects` 中 DOT 杀敌后的 `log.saveLogs()` 调用，依赖 `endCombat` 内部的 `saveLogs()`（已 await），避免同一次战斗结束时日志被保存两次。

### P2-39. forms/store.ts 创建完整 ICombatContext 但只使用部分，违反接口最小化
- **涉及文件**：[forms/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/forms/store.ts#L22-L44)
- **修复方案**：采用直接依赖模式，移除 `createCombatContext()` 调用，改为直接 `useCharacterStore()` 和 `useLogStore()`，避免引入 6 个 Store 的不必要依赖。

### P2-40. useEnemyAction 中多次调用 ctx.skill.getSkill 未缓存结果
- **涉及文件**：[useEnemyAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useEnemyAction.ts#L335)
- **修复方案**：在 `enemyAction` 函数开头缓存 `const cachedFullSkill = ctx.skill.getSkill(decision.skillId);`，在各分支（debuff 判定、攻击技能处理）中复用，避免重复查询。

### P2-41. useBossMechanics 召唤小怪位置分配只检查前排，前排满时位置重叠
- **涉及文件**：[useBossMechanics.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useBossMechanics.ts#L166-L178)
- **修复方案**：前排 3 个位置满后检查后排，参考 `useInitiative.assignEnemyPositions` 的实现逻辑。先查找前排空位，找不到再查找后排空位，避免位置重叠。

### P2-42. usePlayerAction 在 composable 中直接调用 useToast，产生副作用
- **涉及文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L1023)
- **修复方案**：移除 `useToast().show(...)` 直接调用，改为通过 EventBus 发射 `INVENTORY_FULL` 事件，由 UI 组件监听并显示 toast，遵循"Store/Composable 不直接操作 DOM"规范。

### P2-43. usePassiveSkills 被动资源生成使用 'skill' 来源，受 skill 上限限制
- **涉及文件**：[usePassiveSkills.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePassiveSkills.ts#L170)
- **修复方案**：被动技能触发的资源生成统一使用 `'passive'` 来源（新增 ResourceSource），并在各资源系统（Focus/Rage/Fury/RunicPower）中配置 `passive: Infinity` 上限，避免被 skill 来源上限截断。

### P2-44. combat/db.ts mapStorageToLogs 手动映射字段，易遗漏新增字段
- **涉及文件**：[combat/db.ts](file:///d:/openSource/wow_dnd/src/modules/combat/db.ts#L20-L27)
- **修复方案**：使用展开运算符 `{ ...log, actorType: ..., eventType: ..., targetType: ... }` 复制所有字段，仅对需要类型收窄的枚举字段（Storage 中为 string，运行时为字面量联合）显式断言，新增字段时自动继承。

### P2-45. useInitiative 玩家与敌人速度计算来源不一致
- **涉及文件**：[useInitiative.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useInitiative.ts#L78-L90)
- **修复方案**：统一使用 `??` 操作符，避免 `dex=0` 时被 `||` 吞掉。玩家速度 `ctx.character.effectiveStats.dex ?? 0`，敌人速度 `e.stats?.dex ?? 5`，并应用 `getSpeedMod` 效果修正。

### P2-46. combat/store.ts 中 saveLogs 多处未 await
- **涉及文件**：[combat/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L206)、[combat/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L309)
- **修复方案**：`endCombat` 中 `await log.saveLogs();`（已为 async 函数）；`startCombat` 为同步函数，使用 `log.saveLogs().catch(err => console.error(...))` 显式处理错误，避免 unhandled rejection。

### P2-47. combatContext.ts 未提供 dispose 方法清理资源
- **涉及文件**：[forms/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/forms/store.ts#L22-L26)
- **修复方案**：通过 P2-39 的修复（forms/store.ts 不再调用 `createCombatContext()`）间接解决，forms 模块直接依赖需要的 Store，避免创建完整 ICombatContext 的开销和潜在内存泄漏。

### P2-48. equipment/store.ts reapplySetBonuses 过滤条件吞掉 value=0 的套装奖励
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L263-L289)
- **修复方案**：将 `b.bonus.bonus.stat && b.bonus.bonus.value` 改为显式判断 `bonus.stat !== undefined && bonus.value !== undefined`，避免 `value=0`（纯效果触发型）被 falsy 过滤掉。

### P2-49. equipment/store.ts totalStats 对 bonus 的 key 无类型守卫，可能产生 NaN
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L177-L188)
- **修复方案**：定义 `validStatKeys: ReadonlySet<keyof Stats>` 白名单，`Object.keys(bonus).forEach(key => { if (validStatKeys.has(key as keyof Stats)) { ... } })`，防止 bonus 含非 Stats 字段时 `undefined += number` 产生 NaN 污染。

### P2-50. inventory/store.ts persistInventory 静默吞掉错误，UI 与 DB 状态不一致
- **涉及文件**：[inventory/store.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/store.ts#L177-L192)
- **修复方案**：通过两条路径修复：(1) 通过 `errorReporter.report` 上报到错误日志（防抖批量写入 localStorage），便于全局监测；(2) 同步设置 `persistError` ref，供 UI 通过 watch 监听并提示用户"保存失败，请重试"。写入成功时重置为 null。

### P2-51. character/store.ts createCharacter 中 initializeCharacterSkills 在持久化之前执行
- **涉及文件**：[character/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L166-L187)
- **修复方案**：调整执行顺序为：创建角色数据 → 更新 store 状态 → `saveCharacterListItem` → `persistCharacter` → `initializeCharacterSkills`。先持久化基础数据，再执行跨模块技能初始化，避免 `initializeCharacterSkills` 抛异常时角色已在 store 但未持久化。

### P2-52. character/store.ts deleteCharacter 级联删除非事务性
- **涉及文件**：[character/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L263-L272)、[CharacterLifecycleService.ts](file:///d:/openSource/wow_dnd/src/services/CharacterLifecycleService.ts#L108-L125)
- **修复方案**：`cascadeDeleteCharacter` 使用 `Promise.allSettled` 并行删除 6 个模块数据，汇总失败结果，任一失败则抛出聚合错误；`deleteCharacter` 用 try/catch 包裹，失败时记录日志并允许重试。

### P2-53. inventory/store.ts useItem 对 bonus 的应用是永久的
- **涉及文件**：[inventory/store.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/store.ts#L419-L431)
- **修复方案**：采用"增加日志明确提示"方案。保留 `applyBonus` 调用作为向后兼容，但在 DEV 模式输出 `console.warn`，提示该消耗品配置了 bonus 字段将永久叠加，建议改为 buff 系统实现临时增益。

### P2-54. shop/store.ts buyItem 中商店库存更新失败不回滚金币和背包
- **涉及文件**：[shop/store.ts](file:///d:/openSource/wow_dnd/src/modules/shop/store.ts#L426-L471)
- **修复方案**：将扣金币、加背包、减库存三步视为事务，第 3 步失败时回滚前两步：返还金币（`gainGold`）、从背包移除物品（`removeItem`），并通知 UI。

### P2-55. equipment/store.ts equipItem 步骤 6（applyBonus）失败后无回滚
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L522-L553)
- **修复方案**：在 catch 中完整回滚：移除新装备（`equipment.value[slot] = null`）、从背包加回新装备（`inventoryAddItemCallback`）、装回旧装备（`equipment.value[slot] = previousEquipped`）并应用旧装备 bonus。

### P2-56. skill/db.ts toSkill 中 targetType 使用 `as` 断言 + `|| undefined` 吞掉空字符串
- **涉及文件**：[skill/db.ts](file:///d:/openSource/wow_dnd/src/modules/skill/db.ts#L193)、[skill/service.ts](file:///d:/openSource/wow_dnd/src/modules/skill/service.ts)
- **修复方案**：在 `skill/service.ts` 中新增 `isValidTargetType` 类型守卫和 `VALID_TARGET_TYPES` 集合，`db.ts` 中使用 `isValidTargetType(data.targetType) ? data.targetType : undefined` 替代 `as Skill['targetType'] || undefined`，运行时校验合法性。

### P2-58. equipment/store.ts doUnequip 警告日志但继续执行，inventoryAddItemCallback 未注入时装备仍被卸下
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L430-L433)
- **修复方案**：回调未注入时直接抛出错误 `throw new Error('[EquipmentStore] inventoryAddItemCallback 未注入，无法卸下装备。请检查 GameBootstrap 初始化流程。')`，阻止卸下操作，避免装备被卸下后无处可去导致丢失。

### P2-59. inventory/db.ts saveItemTemplate 的 effect 字段使用 `as unknown as` 双重断言
- **涉及文件**：[inventory/db.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/db.ts#L98-L101)
- **修复方案**：显式构造存储对象 `effect: item.effect ? { type: item.effect.type, value: item.effect.value } : null`，避免 `as unknown as ItemStorage['effect']` 双重断言。

### P2-60. animateBossIntro 使用不可取消的 setTimeout
- **涉及文件**：[animation/service.ts](file:///d:/openSource/wow_dnd/src/modules/animation/service.ts#L381-L440)
- **修复方案**：函数返回 `BossIntroAnimationController` 控制器对象，包含 `cancel()` 方法。内部 `closeTimer` 保存定时器 ID，`cancel()` 调用 `clearTimeout(closeTimer)` 并清理动画实例，供调用方在组件卸载时取消。

### P2-61. 动画函数不返回控制器，组件卸载时无法取消
- **涉及文件**：[animation/service.ts](file:///d:/openSource/wow_dnd/src/modules/animation/service.ts)
- **修复方案**：所有 `animate*` 函数（`animateShake`、`animateFlash`、`animateFloatText`、`createParticleBurst` 等）返回 anime.js 的 `JSAnimation` 或 `Timeline` 实例，供调用方在 `onUnmounted` 中调用 `.pause()` 取消动画。

### P2-62. base store 选中状态在角色切换时可能泄漏
- **涉及文件**：[base/store.ts](file:///d:/openSource/wow_dnd/src/modules/base/store.ts#L117-L123)
- **修复方案**：在 `initialize()` 中调用 `resetSelection()`，或在 GameBootstrap 的角色切换流程中显式调用，避免 `selectedFactionId`/`selectedRaceId`/`selectedClassId` 在角色切换时泄漏。

### P2-63. base db 的 update/delete 存在 TOCTOU 竞态
- **涉及文件**：[base/db.ts](file:///d:/openSource/wow_dnd/src/modules/base/db.ts#L61-L95)、[base/db.ts](file:///d:/openSource/wow_dnd/src/modules/base/db.ts#L148-L273)
- **修复方案**：所有 `update*`/`delete*` 方法（`updateFaction`、`deleteFaction`、`updateRace`、`deleteRace`、`updateClass`、`deleteClass`）使用 Dexie 显式事务 `gameDb.transaction('rw', gameDb.config_xxx, async () => { ... })` 包裹 get-then-write，避免 TOCTOU 竞态。

### P2-64. base store 不监听 GAME_DATA_UPDATED，外部更新后数据过期
- **涉及文件**：[base/store.ts](file:///d:/openSource/wow_dnd/src/modules/base/store.ts#L322-L331)
- **修复方案**：在 `initialize()` 中注册 `GAME_DATA_UPDATED` 监听器，过滤相关类型后调用 `loadAllData()`，并在 dispose 时注销。监听器中忽略 'bulk' action 避免循环刷新。

### P2-65. createCrudActions 事件发射顺序错误
- **涉及文件**：[base/store.ts](file:///d:/openSource/wow_dnd/src/modules/base/store.ts#L54-L95)
- **修复方案**：调整 `create`/`update`/`remove` 的执行顺序为：写入 DB → `loadFn` 重新加载本 store → emit 事件。确保其他模块的监听器回调中查询 base store 数据时看到的是最新状态，而非 DB 已更新但 store 尚未刷新的过期状态。

### P2-66. audio store dispose 不 flush，快速角色切换丢失设置
- **涉及文件**：[audio/store.ts](file:///d:/openSource/wow_dnd/src/modules/audio/store.ts#L108-L115)
- **修复方案**：在 `dispose` 中同步调用 `flushSave().catch(err => console.error(...))`，强制写入未保存的设置变更，避免 300ms 去抖窗口内角色切换导致设置丢失。

### P2-67. audio service 的 bgmTransitionTimer 可能未在 dispose 时清理
- **涉及文件**：[audio/service.ts](file:///d:/openSource/wow_dnd/src/modules/audio/service.ts#L1445-L1447)
- **修复方案**：在 `dispose` 方法中包含 `if (this.bgmTransitionTimer) { clearTimeout(this.bgmTransitionTimer); this.bgmTransitionTimer = null; }`，`stopBgm` 中同样清理，避免角色切换后回调指向已销毁的 service 状态。

### P2-68. ExplorationView.vue 中 rafId 未在组件卸载时清理
- **涉及文件**：[ExplorationView.vue](file:///d:/openSource/wow_dnd/src/components/ExplorationView.vue#L254-L260)
- **修复方案**：添加 `onUnmounted` 钩子，`if (rafId) { cancelAnimationFrame(rafId); rafId = 0; }`，清理未执行的 rAF 回调，避免卸载后回调执行访问已卸载组件的响应式状态。

### P2-69. CharacterLifecycleService 中 cascadeDeleteCharacter 使用 Promise.all 可能导致部分删除状态
- **涉及文件**：[CharacterLifecycleService.ts](file:///d:/openSource/wow_dnd/src/services/CharacterLifecycleService.ts#L108-L125)
- **修复方案**：使用 `Promise.allSettled` 替代 `Promise.all`，确保所有 6 个删除操作都完成。汇总失败结果 `failures = results.filter((r): r is PromiseRejectedResult => r.status === 'rejected')`，任一失败则抛出聚合错误。

### P2-70. App.vue 暴露 gameState 到 window 存在安全隐患
- **涉及文件**：[App.vue](file:///d:/openSource/wow_dnd/src/App.vue#L138-L141)
- **修复方案**：使用 `if (import.meta.env.DEV) { window.__gameState = gameState; }` 条件包裹，生产环境不暴露，避免第三方脚本绕过角色选择流程直接修改 `gameState.value`。

### P2-71. GameBootstrap 中 dispose 列表与 initialize 列表不对称
- **涉及文件**：[GameBootstrap.ts](file:///d:/openSource/wow_dnd/src/services/GameBootstrap.ts#L104-L118)
- **修复方案**：`dispose` 方法对显式实现 `Disposable` 接口的 Store（combat/exploration/audio）调用 `dispose()`，并清理 inventory 回调。其他 Store（log/inventory/equipment/skill/map/quest）不实现 dispose 的理由已通过注释说明（无 EventBus 监听器、定时器或订阅需清理）。

### P2-72. ITEM_TYPES 中 quest 类型 stackable 与 maxStack 逻辑矛盾
- **涉及文件**：[config/inventory.ts](file:///d:/openSource/wow_dnd/src/config/inventory.ts#L21-L22)
- **修复方案**：将 `quest: { stackable: true, maxStack: 1 }` 改为 `quest: { stackable: false, maxStack: 1 }`，与 `maxStack: 1` 语义一致，且符合任务物品设计上不可堆叠的约定。

### P2-73. 稀有度颜色在两处重复定义，存在同步风险
- **涉及文件**：[config/inventory.ts](file:///d:/openSource/wow_dnd/src/config/inventory.ts#L30-L36)、[config/console-style.ts](file:///d:/openSource/wow_dnd/src/config/console-style.ts#L16-L41)
- **修复方案**：以 `RARITY_CONFIG` 作为唯一颜色源，`console-style.ts` 通过 `import { RARITY_CONFIG } from './inventory'` 引用，并提供 `buildRarityConsoleStyle()` 函数动态构建 `Record<ItemRarity, string>`，避免两处手动同步。

### P2-74. config_class_passives.ts 中 effect.value 单位混乱
- **涉及文件**：[character/types.ts](file:///d:/openSource/wow_dnd/src/modules/character/types.ts)
- **修复方案**：扩展 `PassiveEffect.value` 的 JSDoc，明确单位约定：`resource_gen` 为绝对数值，`heal`/`stat_modifier`/`damage_reduction`/`buff` 为百分比小数，并注明 `heal` 的分母随触发时机变化（on_attack 按造成伤害，on_turn_start/on_damaged/on_low_hp 按最大生命）。

### P2-75. config_class_talents.ts 中 effect.type='special' 未文档化
- **涉及文件**：[config_class_talents.ts](file:///d:/openSource/wow_dnd/src/data/config_class_talents.ts)、[character/talents/types.ts](file:///d:/openSource/wow_dnd/src/modules/character/talents/types.ts)、[character/talents/service.ts](file:///d:/openSource/wow_dnd/src/modules/character/talents/service.ts)、[skill/store.ts](file:///d:/openSource/wow_dnd/src/modules/skill/store.ts)
- **修复方案**：新增 `healing_multiplier` 类型作为一等公民类型，替代 6 处治疗效果提升天赋的 `special` 类型。在 `accumulateEffect` 中处理该类型并累加到 `TalentEffectSummary.healingMultiplier`，在 `skill/store.ts` 的 `health_restore` 分支中应用 `healingMultiplier` 加成。

### P2-76. config_skills.ts 中技能结构不统一（mpCost vs resourceCost）
- **涉及文件**：[skill/types.ts](file:///d:/openSource/wow_dnd/src/modules/skill/types.ts)、[config_skills.ts](file:///d:/openSource/wow_dnd/src/data/config_skills.ts)
- **修复方案**：将 `Skill.mpCost` 改为可选字段（`mpCost?: number`），资源型技能可不配置；消费方使用 `?? 0` 处理。移除 69 个资源型技能中冗余的 `mpCost: 0` 字段。

### P2-77. errorReport.ts 的 localStorage 持久化存在性能问题
- **涉及文件**：[utils/errorReport.ts](file:///d:/openSource/wow_dnd/src/utils/errorReport.ts#L142-L210)
- **修复方案**：引入防抖 + 内存缓冲区批量写入。`persistToLocalStorage` 将记录追加到 `pendingPersist` 缓冲区，通过 `setTimeout` 延迟批量写入（`PERSIST_DEBOUNCE_MS`），避免错误爆发时 localStorage 同步 IO 阻塞主线程。提供 `flushPersist()` 同步刷新方法供测试/诊断场景调用。

### P2-78. toRawData 使用 JSON 序列化无法处理特殊类型
- **涉及文件**：[utils/db-helpers.ts](file:///d:/openSource/wow_dnd/src/utils/db-helpers.ts#L24-L34)
- **修复方案**：优先使用 `structuredClone`（保留 Date/Map/Set/RegExp 等特殊类型），失败时回退到 `JSON.parse(JSON.stringify())`（Vue Proxy 嵌套响应式或不可克隆类型场景）。

### P2-79. validate.ts 校验逻辑不完整
- **涉及文件**：[data/validate.ts](file:///d:/openSource/wow_dnd/src/data/validate.ts)
- **修复方案**：扩展校验函数，新增 `validateItemSetReferences()`（装备 setId 引用校验）、`validateSkillBuffs()`（技能 buffs type 合法性校验）、`validateBossPhasesOrder()`（Boss 阶段 hpThreshold 降序校验）、`validateItemSets()`（套装 ID 唯一性校验）、`validateQuestData()`（任务 objectives 引用校验）。所有校验在开发环境自动执行。

### P2-80. config/database.ts 类型再导出方式不规范
- **涉及文件**：[config/database.ts](file:///d:/openSource/wow_dnd/src/config/database.ts#L6-L13)
- **修复方案**：删除无意义的 `export type` 再导出语句，仅保留 `import type` 用于常量类型注解。调用方直接从 `@/modules/data` 导入类型，避免同一类型存在两个导入入口。

### P2-81. config_bosses.ts 中 phases.hpThreshold 未强制降序
- **涉及文件**：[data/validate.ts](file:///d:/openSource/wow_dnd/src/data/validate.ts#L151-L181)
- **修复方案**：在 `validate.ts` 中新增 `validateBossPhasesOrder()` 校验函数，遍历所有 Boss 的 `phases` 数组，检查 `hpThreshold` 严格降序（如 0.5 → 0.25 → 0），开发期发现排序问题，避免阶段切换逻辑跳过中间阶段或反复触发。

---

## 延后/保留的问题

### P2-38. usePlayerAction 中 playerSkill 函数过长（约 365 行）— 延后处理
- **涉及文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L429-L793)
- **处理决策**：延后处理。该函数约 360 行，包含 AOE 伤害、单目标伤害、buff/debuff 施加、治疗等多种逻辑分支。拆分需要提取多个内部函数并处理共享闭包变量，风险较高且属于代码质量问题而非功能 bug。建议在独立的重构会话中处理，配合完整测试覆盖。
- **当前状态**：所有 3906 测试通过，功能正常，仅代码规范层面的问题。

### P2-57. character/store.ts handleDeath 自动复活，可能不符合游戏设计 — 有意设计保留
- **涉及文件**：[character/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L478-L490)
- **处理决策**：有意设计保留。代码注释（478-481 行）明确记录："角色死亡后自动复活...这是有意设计：死亡惩罚为经验清零 + 半血复活，而非永久死亡"。`handleDeath` emit `CHARACTER_DEATH` 后调用 `resurrect()`，由 `computeResurrection` 统一处理经验清零等状态重置。若未来产品需求变更为需用户确认死亡界面，可将 `resurrect` 拆分为独立 UI 触发。

---

## 修复统计

| 类别 | 数量 | 问题编号 |
|------|------|----------|
| 已修复 | 48 | P2-32 ~ P2-81（除 P2-38、P2-57） |
| 延后处理 | 1 | P2-38（playerSkill 函数拆分，大型重构） |
| 有意设计保留 | 1 | P2-57（handleDeath 自动复活） |
| **合计** | **50** | |

---

## 关键修复要点

1. **事务完整性**：P2-51/52/54/55/63 修复了角色创建、删除、商店购买、装备穿脱、基础数据 CRUD 的事务性问题，确保数据一致性。
2. **资源清理**：P2-60/61/66/67/68/71 修复了动画定时器、音频定时器、rAF 回调、Store dispose 的资源泄漏问题。
3. **类型安全**：P2-33/34/49/56/59/78 修复了类型断言绕过检查、NaN 污染、双重断言等问题。
4. **错误处理**：P2-32/50/77 修复了异常吞掉、静默失败、性能瓶颈问题。
5. **数据校验**：P2-79/81 扩展了开发期数据完整性校验，覆盖套装 ID、装备 setId、技能 buffs、Boss 阶段顺序等。
6. **配置一致性**：P2-72/73/76 修复了配置矛盾、颜色重复定义、技能结构不统一问题。
7. **游戏平衡**：P2-43/48/75 修复了被动资源生成上限、套装奖励过滤、治疗效果天赋失效问题。
