# 第三轮 P3 批次修复归档

> 修复时间：2026-08-10
> 验证结果：tsc 0 错误 / eslint 0 错误 0 warnings / 模块测试 173 文件 5338 项全通过
> 修复项数：12 项 P3 + 6 项验证排除 + 8 项回退为设计保留

---

## P3 轻微（12 项）

### P6-005 [P3] addEffectToContainer 的 additive/independent 策略等价
- 文件: `src/modules/combat/effects/container.ts`
- 修复: 添加文档注释说明 additive 与 independent 当前走同一分支的语义等价性，以及未来拆分方向。刷新时保留旧 id 不变避免外部引用断链。

### P6-055 [P3] 购买上限校验按整单 quantity 拒绝
- 文件: `src/modules/shop/store.ts`
- 修复: 有剩余额度时按 `remaining` 部分购买并提示，仅剩 0 时才拒绝。`quantity` 调整为 `remaining`，`totalPrice` 相应调整。

### P6-057 [P3] allocateStat 边界：effective 已达 MAX 时仍消耗点数
- 文件: `src/modules/character/store.ts`
- 修复: 上限判定从 `character.value.allocatedStats[stat] >= MAX_STAT` 改为 `effectiveStats.value[stat] >= MAX_STAT`，考虑 potionStats/bonusStats 聚合值。

### P6-106 [P3] 多选项事件"安全离开"选项回血
- 文件: `src/modules/exploration/service.ts`
- 修复: "神秘祭坛"第二选项"安全离开"的 effect 从 `{ type: 'heal', amount: lv * 2 }` 改为 `{ type: 'heal', amount: 0 }`。

### P6-107 [P3] map store getState 返回对象未冻结
- 文件: `src/modules/map/store.ts`
- 修复: `getState()` 返回对象用 `Object.freeze()` 包裹，防止外部 mutate。

### P6-152 [P3] handleBattleTriggered/onLevelUp 定时器清理依赖调用方
- 文件: `src/composables/useGameActions.ts`
- 修复: `cleanup()` 添加幂等保护（`if (disposed) return`），多次调用安全。

### P6-153 [P3] character 计算属性回退对象未类型收窄
- 文件: `src/composables/useGameActions.ts`
- 修复: 回退对象从 `{ name: '...', level: 1 }` 扩展为包含 `classId/raceId/factionId` 完整字段。

### P6-154 [P3] ErrorHandler.wrapAsync 已 deprecated 但仍暴露
- 文件: `src/services/ErrorHandler.ts`
- 修复: 移除 `wrapAsync` 方法（全项目无调用方），同步移除测试中 8 个相关用例。

### P6-155 [P3] main.ts 音频初始化失败无重试入口
- 文件: `src/main.ts`
- 修复: 初始化失败后重新注册一次性 `pointerdown`/`keydown` 监听，为用户提供一次自动重试机会。

### P6-202 [P3] ConfigCache 加载失败时 Promise 去重状态不一致
- 文件: `src/modules/config/cache.ts`
- 修复: `_doLoadTalentTrees`/`_doLoadPassives`/`_doLoadSetDefinitions` 三个方法添加 catch 块，失败时复位缓存状态（`_xxx = null`），确保下次调用可重试。

### P6-204 [P3] getExpForLevel 边界语义
- 文件: `src/utils/calculations.ts`
- 修复: `level > MAX_LEVEL` 时返回 `Infinity`（原返回 `LEVEL_EXP_REQUIREMENTS[MAX_LEVEL]`），明确表示已封顶不可再升级。同步更新测试断言。

### P6-056 [P3→设计保留] capabilityRegistry 占位处理器为死代码
- 文件: `src/modules/item/capabilityRegistry.ts`
- 结论: 验证后为设计保留。占位处理器是"类型闭合 + 未来扩展点"，业务逻辑走 service 层，注释已明确说明。

---

## 验证排除（6 项）

| 编号 | 说明 |
|------|------|
| P6-002 | `enemies` 是从 `enemyIds` 派生的 computed，push 后自动反映。 |
| P6-008 | 测试明确断言空敌人分支不 emit，异常防御分支设计如此。 |
| P6-103 | 兜底发放金币和经验替代物品，不 emit count=0 事件。 |
| P6-104 | `CAMP_HEAL_HP = Number.MAX_SAFE_INTEGER`，receiveHeal 限幅实现完全恢复。 |
| P6-156 | BaseIcon 已有完善的 SVG 净化逻辑（script/on*/javascript:/behavior/-moz-binding）。 |
| P6-157 | 所有 querySelector 调用已有 `instanceof HTMLElement` 守卫。 |

---

## 回退为设计保留（8 项）

| 编号 | 说明 |
|------|------|
| P6-056 | 占位处理器为类型闭合 + 未来扩展点，业务逻辑走 service 层。 |
| P6-150 | popupMounted 与 showXxx 状态冗余：已有 P5-029 修复 shop 路径，其余弹窗无双状态同步问题。 |
| P6-151 | init 已有 try-catch + disposed 标记，registerUICallbacks 在 dispose 时由 explorationStore.dispose 清理。 |
| P6-158 | CharacterInfoPopup 抽离需较大重构，CombatPopup 已部分抽离，保持现状。 |
| P6-159 | refreshData await 已有 try-catch 外层保护。 |
| P6-160 | configCache.loadAll 有 finally 清理 + catch 兜底返回空数组。 |
| P6-161 | INVENTORY_FULL 内联文案简单直接。 |
| P6-203 | errorReport dispose flush 低风险观察项。 |

---

## 测试更新

- `test/services/ErrorHandler.test.ts`: 移除 wrapAsync 的 8 个测试用例（方法已删除）。
- `test/shop/store.test.ts`: 限购剩余 > 0 测试从"返回 false"改为"部分购买返回 true"。
- `test/character/store.test.ts`: allocateStat 测试移除 `computeEffectiveStats not called` 断言（P6-057 修复后为预期调用）。
- `test/utils/calculations.test.ts`: getExpForLevel 超出 MAX_LEVEL 断言从 `LEVEL_EXP_REQUIREMENTS[MAX_LEVEL]` 改为 `Infinity`。
