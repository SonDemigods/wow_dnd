# 已修复问题归档

> 修复时间：2026-07-15
> 归档记录：本轮修复覆盖 22 个问题，测试验证 141 文件 / 3574 测试全部通过。

---

## P0 - 紧急修复（3/3 ✅）

### 1. 敌人生命恢复绕过 Store 响应式更新（Bug）✅
- **文件**：[useInitiative.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useInitiative.ts#L218)
- **修复**：将直接赋值 `enemy.hp = ...` 改为调用 `ctx.enemy.takeDamage(eId, -tickRes.regenAmount)`，通过 enemy Store 的展开赋值方式更新 HP，触发 Vue 响应式追踪。

### 2. 任务完成奖励与状态持久化缺乏事务性（Bug）✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/quest/store.ts#L254-L282)
- **修复**：调换执行顺序——先 `await _updateAndPersistInstance` 持久化任务状态，成功后再 `await _grantQuestRewards` 发放奖励。

### 3. 数据库/数据初始化失败静默吞没 ✅
- **文件**：[main.ts](file:///d:/openSource/wow_dnd/src/main.ts#L64-L68)、[main.ts](file:///d:/openSource/wow_dnd/src/main.ts#L87-L89)
- **修复**：数据库打开失败添加 `errorReporter.report()` + `throw error` 阻止挂载；数据初始化失败添加 `errorReporter.report()` 确保错误被记录。

---

## P1 - 高优先级修复（11/13 ✅，2 个暂缓）

### 4. `gainGold` 接受负值，绕过余额校验 ✅
- **文件1**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L326-L331)：`amount === 0` → `amount <= 0`
- **文件2**：[service.ts](file:///d:/openSource/wow_dnd/src/modules/character/service.ts#L217)：添加 `Math.max(0, ...)` clamp

### 5. 荆棘反伤伤害音效事件使用管线前原始伤害值 ✅
- **文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L363)：`amount: finalDamage` → `amount: actualDamage`
- 同文件第 642 行：`amount: skillDamage` → `amount: actualSkillDamage`

### 8. CombatPopup computed 异常吞没 ✅
- **文件**：[CombatPopup.vue](file:///d:/openSource/wow_dnd/src/components/popup/CombatPopup.vue#L448-L476)
- **修复**：添加 `errorHandler.report(e)` 调用

### 9. 战斗结束时经验/金币发放未 await ✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L76)
- **修复**：`endCombat` 改为 `async`，用 `await Promise.all([gainExp, gainGold])` 等待奖励持久化

### 10. v-for 内联 filter 创建中间数组 ✅
- **文件**：[CharacterCreate.vue](file:///d:/openSource/wow_dnd/src/components/CharacterCreate.vue#L27)
- **修复**：提取为 `displayFactions` computed 属性

### 11. 探索网格拖拽缺少节流 ✅
- **文件**：[ExplorationView.vue](file:///d:/openSource/wow_dnd/src/components/ExplorationView.vue#L147-L158)
- **修复**：使用 `requestAnimationFrame` 节流 panX/panY 更新

### 12. `v-html` 渲染 SVG 净化覆盖面偏窄 ✅
- **文件**：[BaseIcon.vue](file:///d:/openSource/wow_dnd/src/components/common/BaseIcon.vue#L53-L64)
- **修复**：`sanitizeSvgBody` 增加对 `behavior:` 和 `-moz-binding:` CSS 属性注入的正则防御

### 13. ShopPopup 中 v-for 使用 index 作为 key ✅
- **文件**：[ShopPopup.vue](file:///d:/openSource/wow_dnd/src/components/popup/ShopPopup.vue#L60-L61)
- **修复**：`:key="index"` → `:key="entry.item.itemId"`

### 14. base 模块 CRUD 返回 `boolean` 丢弃错误信息 ✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/base/store.ts#L54-L93)
- **修复**：确认已存在 `errorHandler.report()` 调用，无需额外修改

### 15. shop 模块商店初始化错误无用户反馈 ✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/shop/store.ts)、[ShopPopup.vue](file:///d:/openSource/wow_dnd/src/components/popup/ShopPopup.vue)
- **修复**：`init`、`openShop`、`refreshShop`、`loadShopItems` 均添加 `errorHandler.report(err, '加载商店失败')`

### 16. enemy Store `createEnemy` 返回 null 混淆"未找到"与"数据库错误" ✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/enemy/store.ts#L71-L73)
- **修复**：catch 块添加 `errorHandler.report(e, '创建敌人失败')`

### 6. enemy-boss 双向循环依赖 ⏸️ 暂缓
- 完整的"方案 A — 组合模式"架构设计文档已编入 [current.md](file:///d:/openSource/wow_dnd/doc/fixed/current.md)，待后续重构

### 7. services/ 层和组件绕过模块 index.ts ⏸️ 暂缓
- 涉及 35+ 处改动，属大规模重构，待后续按模块逐步收口

---

## P2 - 中优先级修复（2/7 ✅）

### 21. skill buff 施加与伤害判定耦合过紧 ✅
- **文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L676-L678)
- **修复**：移除 `&& result.damage` 条件，buffs 施加不再依赖伤害判定

### 22. 角色复活时经验值被重复清零 ✅
- **文件**：[store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L438-L460)
- **修复**：移除 `handleDeath` 中的 `exp = 0`，由 `computeResurrection` 统一处理

---

## P3 - 低优先级修复（6/12 ✅）

### 24. animation 模块命名标准化 ✅
- **修复**：`combat-effects.ts` → `service.ts`，同步更新 `index.ts` 导入

### 25. audio 模块 organVoice.ts 公共 API 补全 ✅
- **修复**：`index.ts` 补充导出 `OrganStopConfig` 和 `OrganVoice`

### 27. enemy 模块不再导出 BOSS_DROP_TABLE ✅
- **修复**：从 `enemy/index.ts` 移除 `BOSS_DROP_TABLE` 导出

### 28. base 模块不再从 index 重导出 generateId ✅
- **修复**：保留在 `base/service.ts` 内部使用，但 `base/index.ts` 不再导出

### 29. `delete_` 重命名为 `remove` ✅
- **修复**：内部函数重命名，外部 API（`deleteFaction` 等）保持不变

### 30. admin 模块错误处理统一 ✅
- **修复**：4 个 CRUD 操作的 catch 块各添加 `errorHandler.report(error, ...)`

### 33. admin/db 索引搜索回退日志级别修正 ✅
- **修复**：`console.error` → `console.warn`

### 34. 硬编码 localStorageKey 常量化 ✅
- **修复**：提取 `ERROR_LOG_STORAGE_KEY` 常量

---

## 统计

| 优先级 | 已修复 | 暂缓 | 合计 |
|--------|--------|------|------|
| P0 | **3** | 0 | 3 |
| P1 | **11** | 2 | 13 |
| P2 | **2** | 5 | 7 |
| P3 | **6** | 6 | 12 |
| **合计** | **22** | **13** | **35** |

> 暂缓问题：架构重构(#6)、模块 API 边界(#7)、test 目录(#17)、map/shop service(#18)、console 迁移(#19)、文件拆分(#20)、RNG(#23)、路径别名(#26)、性能优化(#31/#32/#35)
