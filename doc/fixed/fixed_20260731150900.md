# #143 遗留：console.ts 根目录 re-export 入口移除归档

> 修复时间：2026-07-31
> 修复批次：#143 遗留（ARCH-8 收尾）
> 测试状态：178 文件 / 4502 测试通过
> tsc 状态：0 错误
> 涉及问题：1 项遗留待评估项已确认修复并归档

---

## 已修复问题

### #143 遗留. console.ts 根目录 re-export 入口仍存在

**问题背景**

QA-7 已将原 `src/modules/console.ts`（单文件 1278 行）拆分为 `src/modules/console/` 子模块（`framework.ts` + `commands/` + `index.ts`），但 `doc/fixed/current.md` 中遗留条目记录"原 `modules/console.ts` 作为 re-export 入口仍保留在根目录，ARCH-8（根目录位置）仍部分存在"，需要后续评估是否完全移除根目录 re-export 入口，统一通过 `@/modules/console` 公共入口导入。

**修复验证**

经核查，#143 遗留问题在 commit `1217ee3 refactor(控制台+战斗): 拆分重构控制台与战斗模块` 中已完整修复，遗留条目描述与当前代码状态不符，本次会话仅做状态确认与文档同步归档：

1. **根目录 re-export 入口已删除**
   - `git log --diff-filter=D --summary -- src/modules/console.ts` 显示 commit `1217ee3` 删除了 `src/modules/console.ts`（1278 行删除）
   - `git ls-files src/modules/console.ts` 返回空，文件不在 git 跟踪中
   - 工作区递归搜索 `console.ts`：无任何匹配文件（仅 `src/config/console-style.ts` 为不相关的样式配置模块）

2. **子模块结构已建立**
   - [src/modules/console/index.ts](file:///d:/openSource/wow_dnd/src/modules/console/index.ts) — 统一公共入口，重导出 `exec` / `initConsole` / `CommandResult` / `CommandDef` / `CommandCategory`
   - [src/modules/console/framework.ts](file:///d:/openSource/wow_dnd/src/modules/console/framework.ts) — 框架核心：类型定义、注册表、`exec` / `initConsole`、公共辅助
   - [src/modules/console/commands/](file:///d:/openSource/wow_dnd/src/modules/console/commands) — 命令实现子模块（character / inventory / combat / skill / exploration / quest / system）

3. **所有引用统一通过公共入口**
   - 全工程搜索 `from ['"](.*)console['"]`：仅 `src/main.ts:13` 一处引用，路径为 `@/modules/console`，符合规范
   - 全工程搜索相对路径引用 `from ['"]\.\.?/console['"]`：无任何匹配
   - `src/modules/console/index.ts` 内部注释中的 `@/modules/console` 为 JSDoc 使用示例，非实际依赖

**与历史归档的关系**

- [fixed_20260731190000.md](file:///d:/openSource/wow_dnd/doc/fixed/fixed_20260731190000.md) 中 ARCH-8 已记录"console.ts 已拆分为 modules/console/ 子模块（framework.ts + commands/ + index.ts）"并标记为"✅ 历史已修复"
- 本次归档为 ARCH-8 的最终收尾，确认根目录 re-export 入口已完全移除，无残留

**涉及文件**

无需修改代码文件，仅同步文档：
- [doc/fixed/current.md](file:///d:/openSource/wow_dnd/doc/fixed/current.md) — 移除 #143 遗留条目，待办统计由 3 项调整为 2 项

**验收结果**

- `pnpm exec tsc --noEmit`：0 错误
- `pnpm exec vitest run`：178 文件 / 4502 测试通过
- `git ls-files src/modules/console.ts`：空（文件已删除）
- 全工程引用扫描：唯一调用方 `src/main.ts` 通过 `@/modules/console` 公共入口导入

**备注**

`vue-tsc` 在当前工具链版本（vue-tsc 3.3.8 + typescript 5.3.2）下存在已知兼容性问题（`ReferenceError: isSatisfiesExpression is not defined`），系工具链问题，与 #143 修复无关，不影响 TypeScript 编译与运行时测试。
