# 已修复问题归档（P2/P3 批次）

> 修复时间：2026-07-15
> 测试验证：141 文件 / 3574 测试全部通过

---

## P2 修复（2 项）

### 17. test/db/ 目录镜像重构 ✅
- **修复**：14 个 `test/db/X.test.ts` 文件移动到 `test/X/db.test.ts`，与 src 模块目录镜像一致
- **文件清单**：audio, base, boss, character, combat, enemy, equipment, exploration, inventory, log, map, quest, shop, skill
- **对齐**：与已有的 `test/admin/db.test.ts` 和 `test/item-template/db.test.ts` 结构一致
- **删除**：空的 `test/db/` 目录已移除

### 18. map 和 shop 模块新增 service.ts 纯函数层 ✅
- **map/service.ts**：提取纯函数 `getLocationById`、`isLocationAccessible`、`getZoneStatus`、`getLocationsByContinent`、`getMapStateKey`、`mapToLocationData`、`clamp`
- **shop/service.ts**：提取纯函数 `calculatePrice`、`computeSellPrice`、`canAffordItem`、`generateShopItems`、`SHOP_TYPE_ITEM_TYPE_MAP`
- **副作用**：map/store.ts 的 `DEFAULT_MAP_VIEW` 被误删后已恢复（`zoomLevel: 1, panX: 0, panY: 0`）
- **测试适配**：更新 `test/map/store.test.ts` 的 `vi.mock` 补充 `clamp` 导出

---

## P3 修复（3 项）

### 31. 探索网格深拷贝开销评估 ✅
- **文件**：[exploration/service.ts](file:///d:/openSource/wow_dnd/src/modules/exploration/service.ts#L478)
- **结论**：已有注释说明"8x8 网格开销可控"，无需代码修改

### 32. 背包整理多次数组复制评估 ✅
- **文件**：[inventory/store.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/store.ts#L523-L525)
- **结论**：`organizeInventory` 通过 `newInventory` 从头构建数组，不使用 `[...inventory.value]` 展开，无可合并操作

### 35. Computed 中每次创建新数组（日志反转）✅
- **文件**：[CombatPopup.vue](file:///d:/openSource/wow_dnd/src/components/popup/CombatPopup.vue#L329-L344)
- **修复**：`logsReversed` computed 改为带缓存的版本，`logs.value` 引用不变时直接返回缓存数组

---

## 暂缓（大规模重构）

| Issue | 内容 | 原因 |
|-------|------|------|
| 19 | console.ts 移至 `src/console/` 并拆分 | 1280 行，需构造独立模块结构 |
| 20 | 超长文件拆分（4 个文件） | 涉及新建 8+ 个 composable/service 文件 |
| 23 | AI 策略 / 伤害管线 seed-based RNG | 设计变更 |
| 26 | 模块路径别名统一（100+ 处） | 批量替换，无功能影响 |
