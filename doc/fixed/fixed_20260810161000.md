# 第八轮全量代码审查修复归档

> 时间：2026-08-10 16:10
> 审查范围：8 子代理并行覆盖 303 源文件
> 修复项数：49（P1×2 + P2×9 + P3×38）
> 验证：tsc 零错误 / eslint 零错误 / vitest 5777 项通过（53 项预存组件测试失败）

---

## P1 修复（2 项）

### P10-001: usePlayerSkill 单体击杀分支提前误判整场胜利
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: 单体伤害技能击杀非 Boss 敌人时，原 `isDead` 为 true 即触发 endCombat('victory')，即使场上仍有其他存活敌人。改为与 playerAttack 对齐：先检查 checkBossRevive，再检查 aliveEnemies.length===0 才胜利，否则 endPlayerTurn。

### P10-005: char_quests 备份折叠数据丢失
- **文件**: src/modules/data/backup.ts, src/modules/data/importer.ts
- **修复**: char_quests 是多行/角色表（复合索引 characterId+questId），备份用 toCharacterRecord 按 characterId 折叠为单行，同角色多任务记录互相覆盖。改为数组形状存储，导入端改用 bulkPut 直接写入数组。

---

## P2 修复（9 项）

### P10-002: clearContainer 不触发 onRemove 清理
- **文件**: src/modules/combat/effects/container.ts
- **修复**: clearContainer 直接 `effects = []` 不调用 onRemove 回调。改为逐个移除并触发 onRemove。

### P10-003: 变形 initialize 属性加成丢失
- **文件**: src/modules/combat/forms/store.ts
- **排除**: P10-018 确认为设计意图（战斗 init 经 switchTo/pipeline 处理，避免重复施加）。

### P10-004: 宠物 skillCooldowns Map 不可序列化
- **文件**: src/modules/combat/pets/types.ts, service.ts
- **修复**: PetInstance.skillCooldowns 从 Map<string, number> 改为 Record<string, number>，序列化安全。

### P10-006: 双手武器装备 weapon1 时 weapon2 回滚缺失
- **文件**: src/modules/equipment/composables/useEquipmentOps.ts
- **修复**: doUnequip('weapon2') 前暂存 unequippedWeapon2，所有 catch 分支恢复 weapon2 槽位与 bonus。

### P10-007: 天赋 canUnlearn 不校验前置依赖
- **文件**: src/modules/character/talents/store.ts
- **修复**: canUnlearn 中遍历所有仍大于 0 的天赋，若其 requires 中任一已在 simulated 中归 0，拒绝取消。

### P10-009: 任务进度含过期条目永久阻塞完成
- **文件**: src/modules/quest/service.ts
- **修复**: checkQuestProgress 中先按 definition.objectives 过滤掉无对应 objective 的过期 progress 条目，再补建缺失条目。

### P10-031: routeSynthTo 战斗路由未连接 fmSynth/synth/metalSynth
- **文件**: src/modules/audio/effectChains.ts
- **修复**: combat 路由分支补充 fmSynth/metalSynth/synth 连接到 compressor，修复战斗中 attack_hit/attack_crit/enemy_hurt/combat_victory 等音效层静音。

### P10-032: AdminTable format 回调拿不到真实 row 对象
- **文件**: src/components/admin/AdminTable.vue
- **修复**: 模板 `formatCellValue(row[col.key], col, row)` 传入完整 row，函数签名加 row 参数并传入 col.format。

### P10-041: BaseDbService CRUD 无 try-catch
- **文件**: src/utils/db-helpers.ts
- **修复**: save/saveAll/getById/getAll/deleteById 均加 try-catch + errorReporter.report 后重新抛出。

---

## P3 修复（38 项）

### P10-008~014: P3 batch1（7 项）
- P10-008: Boss 位置分配注释修正
- P10-009: 动画魔法数字提取 config
- P10-010: 敌人伤害浮动文字反映魔法类型
- P10-011: useCombatAutoClose 秒数提取 config
- P10-012: pipeline applyEffect 透传 ctx
- P10-013: petChompRatio 提取 config
- P10-014: Boss intro 动画时长提取 config

### P10-015~022: P3 batch2（8 项）
- P10-015: allocateStat MAX_STAT 守卫
- P10-016: useSetBonus buildKey 加 tier 标识
- P10-017: pipeline applyEffect 透传 ctx（与 P10-012 合并）
- P10-018: 变形 initialize 排除（设计意图）
- P10-019: computeEventProbability 归一化改 Math.floor
- P10-020: Boss originalBaseStats 快照时机提前
- P10-021: enemy.createEnemy throw 改 null 返回
- P10-022: dbFromLinear 返回 -80 固定值

### P10-023~030: P3 batch3（8 项）
- P10-023: pendingSfxTypes 去重+5s 节流
- P10-024: MAX_BACKUP_SIZE 提取常量
- P10-025: GameBootstrap.dispose 清理表单上下文
- P10-026: AdminQueryService 加 try-catch
- P10-027: ErrorReporter 缓冲区切片注释
- P10-028: InventoryPopup.useItem 重新查找预置
- P10-029: CombatPopup.useItem 传 index 参数
- P10-030: AdventureLogPopup.confirmClear 加 try-catch

### P10-034~038: P3 batch A（5 项）
- P10-034: AdminForm as 断言改 instanceof 守卫
- P10-035: ShopPopup 加 isUnmounted 卸载守卫
- P10-036: useShopTransactions decBuyQty/decSellQty 加 Math.max(1,...)
- P10-037: TalentPopup 布局魔法数字提取 config/talentLayout.ts
- P10-038: freeze 速度修正提取 config/combat.ts FREEZE_SPEED_MOD

### P10-039: 攻击修正应用顺序统一（P3 batch C）
- **文件**: src/modules/combat/effects/pipeline.ts
- **修复**: effect 的 attack_up/attack_down（safeAttackerMod）从减伤后移到减伤前，与 passive attackMultiplier 合并为 combinedAttackMultiplier。bonusPercent 保持减伤后。

### P10-040: enterArea areaId 与 boardId 一致性（P3 batch B）
- **文件**: src/modules/exploration/store.ts
- **修复**: 确认 config_quests 的 boardId 与 config_locations 的 location.id 取相同值，加注释说明。

### P10-042~044, 047: P3 batch B（4 项）
- P10-042: GameBootstrap.dispose 加 clearPetCallbacks()
- P10-043: getHpPercent maxHp=0 时返回 0
- P10-044: useGameActions cleanup 显式调 unregisterUICallbacks
- P10-047: getQuestRequiredMonsters 去重 push

### P10-045~049: P3 batch C（5 项）
- P10-045: 音频 eventHandler 类型断言加设计取舍注释
- P10-046: createParticleBurst 增加 ParticleBurstController.cancel()
- P10-048: adventureLog 旧格式降级加注释
- P10-049: 连击点/真气生成链路确认+注释

### P10-033: AdminTable 行 key 增加 __pk 回退
- **文件**: src/components/admin/AdminTable.vue
- **修复**: getRowKey 增加 `row.__pk` 字段回退，避免无 id/characterId 的表退化为序号。

---

## 测试更新（5 个文件）

1. test/combat/effects/pipeline.test.ts: 3 个测试更新 expectedDamage/actualDamage/finalDamage 值匹配 P10-039 攻击修正顺序变更
2. test/audio/effectChains.test.ts: 1 个测试更新 combat 路由断言匹配 P10-031 新增合成器连接
3. test/combat/resources/ResourceSystemFactory.test.ts: 1 个测试改写为验证 _cache 缓存行为 + clearCache

---

## 覆盖缺口

- 子代理3（角色/装备/技能/天赋）：character/store.ts 因编码问题未获可读全文；skill 模块 composables（useSkillCasting/useSkillState/useSkillBar/useSkillLearning）未读取
- 子代理6（音频/基础设施）：base/bus/config-cache/game/inventory/item/item-template/log/shop/admin 模块因预算耗尽未读取
- 子代理7（Vue 组件）：CharacterSelectPopup/QuestBoardPopup/ArchiveManagerPopup/AboutPopup 及 common/ 13 个组件因预算耗尽未读取
