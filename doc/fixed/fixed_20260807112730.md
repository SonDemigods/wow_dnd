# 战斗系统 P3 低优修复（阶段四）

> 修复时间：2026-08-07
> 修复项数：4（P3-183 / P3-184 / P3-185 / P3-186）
> 验证结果：tsc 0 错误、eslint 0 错误、combat+enemy 测试 1546 项全通过

---

## P3-183：dispose 完整性 + useCombatAnimations 公开导出（P3 低）

- **问题位置**：`store.ts` dispose() 缺少 petStore.reset()；`index.ts` 公开导出 useCombatAnimations
- **修复方案**：
  1. dispose() 追加 `pet.petStore.reset()`，避免角色切换时 petStore 状态残留
  2. useCombatAnimations 导出添加 JSDoc 标注"仅供 CombatPopup.vue 使用"
- **修改文件**：`src/modules/combat/store.ts`、`src/modules/combat/index.ts`

---

## P3-184：伤害回血/事件/Buff 等代码异味（P3 低，6 子项合并）

- **子项 1**：敌人 HOT 通过负伤害实现回血 → 新增 `enemy/store.ts receiveHeal()` 方法，`ICombatCommand.enemy` 接口同步，`useInitiative.ts` 替换 `takeDamage(负值)` 为 `receiveHeal`
- **子项 2**：heal 技能 buffs 字段未应用 → `usePlayerSkill.ts` heal 分支结束前增加 `applySkillBuffs(skill, 'self')` 调用
- **子项 5**：applyBuff DOT 回合数硬编码 → `PassiveEffect` 新增 `turns?: number` 字段，`usePassiveSkills.ts` 改为 `effect.turns ?? 3`
- **修改文件**：`enemy/store.ts`、`combatContext.ts`、`useInitiative.ts`、`usePlayerSkill.ts`、`usePassiveSkills.ts`、`character/types.ts`

---

## P3-185：命名规范与操作符一致性（P3 低）

- **子项 1**：移除 `_` 前缀误用 → `_talentDmgMult`/`_preCritDmg` 改为 `talentDmgMult`/`preCritDmg`（usePlayerSkill.ts、usePlayerItem.ts、usePetAction.ts 共 4 处）
- **子项 2**：`||` 改 `??` → `useCombatLog.ts:100` `speed: ctx.character.effectiveStats.dex ?? 0`
- **子项 3**：提取局部变量消除非空断言 → `usePlayerSkill.ts:162` `skill.generatesResource!.type` 改为提取 `const gen = skill.generatesResource` 后使用 `gen.type`
- **修改文件**：`usePlayerSkill.ts`、`usePlayerItem.ts`、`usePetAction.ts`、`useCombatLog.ts`

---

## P3-186：buildInitiativeOrder 先手语义 + 空数组防御（P3 低）

- **问题位置**：`useInitiative.ts` advanceToNextUnit 对空 unitId 无守卫
- **修复方案**：在 `advanceTurn()` 后增加 `if (!next.unitId) return;` 守卫，防止空先攻数组导致无限递归
- **修改文件**：`src/modules/combat/composables/useInitiative.ts`

---

## 验证结果

| 检查项 | 结果 |
|--------|------|
| `tsc --noEmit --skipLibCheck` | 0 错误 |
| ESLint（修改文件） | 0 错误 |
| combat + enemy 测试 | 1546/1546 通过（51 文件） |
