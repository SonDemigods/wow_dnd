# 第四轮修复归档（P7-001~033）

> 修复时间：2026-08-10
> 来源：第四轮全量代码审查（302 源文件，6 子代理并行）
> 验证：tsc 零错误 / eslint 零警告 / vitest 50 失败（均为预存组件测试 wrapper.emitted() + version.test.ts 问题）

---

## P1 — 严重

### P7-001 runtime_shopSoldItems 表未纳入备份/导入管道
- 文件: src/modules/data/types.ts、src/modules/data/backup.ts、src/modules/data/importer.ts
- 修复: BackupData 增加 `shopSoldItems?: Record<string, ShopSoldItemsStorage>`，collectAllData 读取 `db.runtime_shopSoldItems`，importData 事务表清单 + bulkPutIfNotEmpty 写入。

---

## P2 — 重要

### P7-002 胜利结算中 quest.onEnemyKilled 未 await 且未捕获
- 文件: src/modules/combat/store.ts
- 修复: 改为 `Promise.all(...).catch(err => console.error(...))`。

### P7-003 equipment.unequipItem 的 setBonus 重算未包 try-catch
- 文件: src/modules/equipment/composables/useEquipmentOps.ts
- 修复: 为 unequipItem 的 reapplySetBonuses 加 try-catch，失败时回滚装备状态并 flushPersist。

### P7-004 MigrationService 忽略 importData 的失败结果仍标记迁移成功
- 文件: src/modules/data/migrations/service.ts
- 修复: 校验 importData 返回值，success=false 时不更新 dataVersion 并返回失败结果。

### P7-005 useShopTransactions handleBuy/handleSell 缺 try/catch
- 文件: src/components/popup/composables/useShopTransactions.ts
- 修复: 包装 try/catch，错误时 console.error。

### P7-006 BaseIcon gradient 切换时 re-render 为空操作
- 文件: src/components/common/BaseIcon.vue
- 修复: 缓存键从 `lastLoadedIcon`(仅 icon) 改为 `lastLoadedKey`(icon:gradient)，gradient 变化时重新加载。

---

## P3 — 轻微

### P7-007 先攻排序中玩家与敌人默认速度基准不一致
- 文件: src/config/combat.ts、src/modules/combat/composables/useInitiative.ts
- 修复: 提取 `INITIATIVE_DEFAULT_SPEED = 5` 到 config/combat.ts，敌人默认速度统一使用此常量。

### P7-008 saveLogs 防重入在 forceAll 场景可能漏存尾日志
- 文件: src/modules/combat/composables/useCombatLog.ts
- 修复: forceAll 时若 in-flight，先 await 现有 promise 后再执行一次 forceAll 保存。

### P7-009 applySkillBuffs 自施放型混合技能的敌方减益被跳过
- 文件: src/modules/combat/composables/usePlayerAction.ts
- 修复: 移除 `targetType !== 'self'` 限制，敌方减益分支不再受 self 目标类型阻断。

### P7-010 非伤害类技能仍触发 onAttack 被动钩子
- 文件: src/modules/combat/store.ts
- 修复: 维持设计（技能也算攻击行为），补充注释说明意图。

### P7-011 computeResurrection 硬编码 0.5 应抽取到 config
- 文件: src/config/character.ts、src/modules/character/service.ts
- 修复: 抽取 `RESURRECT_HP_RATIO` / `RESURRECT_MP_RATIO = 0.5` 到 config/character.ts。

### P7-012 selectCharacter 存在过期注释与行为矛盾
- 文件: src/modules/character/store.ts
- 修复: 更新注释，明确 currentCharacterId 在 emit LOGOUT 时已是新值（P3-116 变更后注释已过期）。

### P7-013 equipItem applyBonus 失败分支未调用 flushPersist
- 文件: src/modules/equipment/composables/useEquipmentOps.ts
- 修复: 在 applyBonus catch 分支末尾补充 `cb.flushPersist()`，与其他回滚分支一致。

### P7-014 revealGrid 商店/任务板分支的 visitedCell 计数为死代码
- 文件: src/modules/exploration/store.ts
- 修复: 删除 `isNewlyVisited` 检查与 `visitedCells.value++` 死代码分支。

### P7-015 checkCompletion 要求全格 visited，存在理论不可完成边界
- 文件: src/modules/exploration/store.ts
- 修复: 放宽为 `bossDefeated && visitedCells >= BOSS_SEAL_REQUIRED_CELLS`，boss 击败后即使有未访问格也允许完成。

### P7-016 rest 处理器 campUsed 已用时分支不可达
- 文件: src/modules/exploration/events.ts
- 修复: 保留为防御性兜底并加注释说明。

### P7-017 生成期/损坏存档缺少 playerPosition 越界 clamp
- 文件: src/modules/exploration/db.ts
- 修复: getExplorationData 读档后对 playerPosition 做基于 grid 尺寸的越界校验，越界时回退到 {0,0}。

### P7-018 enemy dex 用原始 dodgeChance 未乘 levelScale
- 文件: src/modules/enemy/service.ts、src/modules/enemy/types.ts
- 修复: dex 改为 `(template.dodgeChance ?? 5) * levelScale * 1.5`，修正类型注释为 0-100 整数百分比。

### P7-019 quest/store completeQuest 的 AND 判定依赖 progress 条目存在
- 文件: src/modules/quest/service.ts
- 修复: checkQuestProgress 开头校验 progress 条目与 definition.objectives 一致性，缺失时补建初始条目。

### P7-020 quest/store _grantQuestRewards 未注入回调时用 console.warn 降级
- 文件: src/modules/quest/store.ts
- 修复: 改用 errorReporter.report 统一错误上报模式。

### P7-021 boss/engine applyPhaseStats 阶段属性为累积连乘
- 文件: src/modules/boss/engine.ts、src/modules/boss/types.ts
- 修复: BossRuntimeState 新增 `originalBaseStats` 字段，applyPhaseStats 首次调用时保存原始 base 属性快照，后续基于快照重算。

### P7-022 console/commands/quest.ts 循环内重复 store 调用
- 文件: src/modules/console/commands/quest.ts
- 修复: 循环外缓存 `const questStore = useQuestStore()` 一次，内部复用。

### P7-023 admin/store saveRecord 用 `id!` 非空断言无运行时守卫
- 文件: src/modules/admin/store.ts
- 修复: 运行时校验 id 非空/非空字符串，不通过时返回 false。

### P7-024 applyVolume 未处理 muted/effectiveSfxVolume
- 文件: src/modules/audio/service.ts
- 修复: applyVolume 改用 `store.effectiveBgmVolume` / `store.effectiveSfxVolume`（含 muted 标志）做 dB 计算。

### P7-025 playSfx/setBgmScene 在 AudioContext 未就绪时丢弃请求
- 文件: src/modules/audio/service.ts
- 修复: tryResume().then() 后若 context 就绪则重放本次请求。

### P7-026 runMigrations 选择条件逻辑欠严谨
- 文件: src/modules/data/migrations/index.ts
- 修复: 改用 `while (currentStep < CURRENT)` + `m.from === currentStep` 显式链式步进。

### P7-027 useCharacterCreation 非空断言无运行时守卫
- 文件: src/composables/useCharacterCreation.ts
- 修复: doCreate 中 re-check 三个值非 null，不通过时 showErrorModal 并 return。

### P7-028 useCharacterCreation 切换种族时未清除非法职业选择
- 文件: src/composables/useCharacterCreation.ts
- 修复: selectRace 中同时重置 `selectedClass` 为 null。

### P7-029 useConfigCrud Dexie 操作缺 try/catch
- 文件: src/components/admin/composables/useConfigCrud.ts
- 修复: confirmDelete/handleFormSubmit 包装 try/catch，错误时 toast 提示，finally 复位弹窗状态。

### P7-030 useConfigCrud 删除键同时使用 id 和 characterId
- 文件: src/components/admin/composables/useConfigCrud.ts
- 修复: 校验 id 非空/非空字符串后才执行删除，空主键时 toast 提示。

### P7-031 MapView 硬编码地图宽高比与平移边距
- 文件: src/config/map.ts（新建）、src/components/MapView.vue
- 修复: 新建 config/map.ts，提取 MAP_ASPECT_RATIO / MAP_ZOOM_STEP / MAP_ZOOM_MIN / MAP_ZOOM_MAX / PAN_BOUND_MARGIN。

### P7-032 InventoryPopup computed 内重复 useInventoryStore() 查找
- 文件: src/components/popup/InventoryPopup.vue
- 修复: setup 顶层缓存 `const inventoryStore = useInventoryStore()`，所有 computed/方法内复用。

### P7-033 useGameActions 战斗触发缺错误处理
- 文件: src/composables/useGameActions.ts
- 修复: handleBattleTriggered 包装 try/catch，错误时 toast 警告。
