# 第十轮全量代码审查归档

> 修复时间：2026-08-12
> 8 子代理覆盖 317 源文件，发现 37 项问题（P1×8 P2×11 P3×18）
> 修复 29 项（P1×8 P2×11 P3×10），暂不修复 P3×8 项（影响低）
> tsc / eslint 零错误，vitest 5781 项通过 / 50 项预存失败无新回归

---

## P1 严重问题（8/8 全部修复）

### P12-001 initBosses 丢失 attackType 和 drops 字段
- 文件：src/modules/data/initializer.ts
- 修复：在 bossData 映射中补充 `attackType: boss.attackType || undefined` 和 `drops: boss.drops || undefined`

### P12-005 BaseResourceSystem.consume 负数消耗导致资源无限增加
- 文件：src/modules/combat/resources/BaseResourceSystem.ts
- 修复：consume 入口加 `if (amount <= 0) return false;` 守卫

### P12-006 forms/store.ts switchTo 未应用 hpMultiplier/damageMultiplier/defenseMultiplier/speedMultiplier
- 文件：src/modules/combat/forms/store.ts
- 修复：FormStoreContext 接口新增 `setFormHpMultiplier` 方法，switchTo 中调用 `formCtx.setFormHpMultiplier(newForm.modifiers.hpMultiplier)`，reset 中重置为 1

### P12-010 useInitiative.ts singleEnemyTurn 敌人死亡时跳过下一个单位回合
- 文件：src/modules/combat/composables/useInitiative.ts
- 修复：`removedIndex < currentInitiativeIndex` 改为 `removedIndex <= currentInitiativeIndex`，使 removedIndex === currentInitiativeIndex 时也递减索引

### P12-011 useInitiative.ts singleEnemyTurn !e 时 initiativeOrder 未过滤 enemyId 导致无限循环
- 文件：src/modules/combat/composables/useInitiative.ts
- 修复：将 initiativeOrder 过滤逻辑从 `if (e)` 块内移到外层，无论 e 是否存在都执行过滤

### P12-012 useEquipmentOps doUnequip cb.addItem 异常时装备物品丢失
- 文件：src/modules/equipment/composables/useEquipmentOps.ts
- 修复：先调用 `cb.addItem()` 添加到背包，成功后再 `removeBonusesFromSlot` 和清空装备槽

### P12-014 exploration/store.ts useCamp 非营地格调用时错误标记当前格为 completed
- 文件：src/modules/exploration/store.ts
- 修复：标记 completed 前校验 `cell.type === 'rest'`

### P12-018 CombatPopup doAction/doSkill/useItem 缺少 try/catch，异常时战斗永久卡死
- 文件：src/components/popup/CombatPopup.vue
- 修复：三处加 try/catch，catch 中 `isAnimating.value = false` + errorHandler.report + toast

---

## P2 中等问题（11/11 全部修复）

### P12-002 职业专属法杖缺少 'usable' 能力标签
- 文件：src/data/config_class_equipment.ts
- 修复：法杖 subtype 注入含 'usable' 的能力组合

### P12-007 forms/store.ts switchTo 无重入保护
- 文件：src/modules/combat/forms/store.ts
- 修复：引入 `isSwitching` 标志，函数入口检查，finally 复位

### P12-008 boss/engine.ts applyPhaseStats 清除 enrage 效果
- 文件：src/modules/boss/engine.ts
- 修复：applyPhaseStats 末尾检查 `boss.runtime.enraged`，重新应用狂暴倍率

### P12-015 bgmSynth.ts 交叉淡变中断时音量卡在 -80dB
- 文件：src/modules/audio/synth/bgmSynth.ts
- 修复：中断淡变后重置 `bgmChannel.volume` 为 `lastBgmDb`

### P12-016 audio/service.ts BGM 振荡器重新启动时使用默认频率
- 文件：src/modules/audio/service.ts
- 修复：新增 `getSceneOscillatorFreq` 方法，按当前场景传入正确频率

### P12-017 ArchiveManagerPopup showResult 标题错误显示"导入失败"
- 文件：src/components/popup/ArchiveManagerPopup.vue
- 修复：新增 `resultTitle` ref，showResult 中赋值，模板绑定 resultTitle

### P12-019 useShopTransactions handleBuy/handleSell 无重入保护
- 文件：src/components/popup/composables/useShopTransactions.ts
- 验证：已有 `isBusy` ref 防重入，无需修改

### P12-027 AdminForm 重新打开"新增"表单残留上次输入
- 文件：src/components/admin/AdminForm.vue
- 修复：watch 从 `[initialData]` 改为 `[initialData, visible]`，visible 为 true 时重置 formData

### P12-028 useConfigCrud 忽略 saveRecord/deleteRecord 的 false 返回值
- 文件：src/components/admin/composables/useConfigCrud.ts
- 修复：检查返回值 `success`，失败时 toast 提示

### P12-029 AdminTable 搜索无防抖竞态
- 文件：src/components/admin/AdminTable.vue
- 修复：新增 `onSearchInput` 函数 + 300ms 防抖定时器

### P12-030 classEquipment classRestriction 从 multiselect 被降级为 select
- 文件：src/components/admin/composables/useConfigTableMeta.ts
- 修复：multiselect 类型保留不覆盖

---

## P3 代码质量（10/18 修复，8 项暂不修复）

### 已修复

| 编号 | 文件 | 修复内容 |
|------|------|----------|
| P12-003 | base/db.ts | getFactionById/getRaceById/getClassById 返回 `?? null` 替代 `cast` |
| P12-004 | gameStateHelper.ts | `id: key` 移到 `...patch` 之后 |
| P12-009 | boss/engine.ts | damage_shield 加 `Math.max(0, ...)` 守卫 |
| P12-013 | equipment/db.ts | occupies 空数组检查 `length > 0` |
| P12-020 | useShopTransactions.ts | canAffordBuy 增加 `buyMaxQuantity > 0` 条件 |
| P12-021 | CombatPopup.vue | playerHpPercent/playerMpPercent 除零保护 |
| P12-022 | InventoryPopup.vue | selectCategory 中 `selectedEntry.value = null` |
| P12-025 | Tag.vue | 添加 `overflow: hidden; text-overflow: ellipsis;` |
| P12-026 | TalentsPanel/TalentPopup | 移除未使用的 containerRef |
| P12-031 | MapView.vue | onConfirmEnter 传入 `characterStore.level` |
| P12-036 | useConfigTableMeta.ts | damage/levelRange 格式化加数组长度校验 |

### 暂不修复（影响低）

| 编号 | 说明 |
|------|------|
| P12-023 | InventoryPopup isEquipped 同 itemId 副本判定，需重构装备状态逻辑 |
| P12-024 | BasePopup 缺少 Escape 键关闭与 a11y，需较大改动影响所有弹窗 |
| P12-032 | AdminForm 空数字字段 Number('') 变 0，影响仅限 admin 后台 |
| P12-033 | main.ts db.open() 失败白屏，需添加错误 UI 组件 |
| P12-034 | GameMain handleOpenInventoryFromCharacter 绕过面板事件 |
| P12-035 | GameMain popupMounted 与 @close 逻辑矛盾 |
| P12-037 | useConfigTableMeta classEquipment type 列缺字典翻译 |

---

## 测试更新

| 文件 | 更新内容 |
|------|----------|
| test/base/db.test.ts | 9 处 `toBeUndefined()` → `toBeNull()`，`not.toBeUndefined()` → `not.toBeNull()` |
| test/components/MapView.test.ts | enterZone 断言改为 `toHaveBeenCalledWith('z1', expect.anything())` |
