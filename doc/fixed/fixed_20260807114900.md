# 战斗系统测试覆盖盲区补齐（阶段五，不含 e2e）

> 修复时间：2026-08-07
> 修复项数：1（P3-187）
> 验证结果：tsc 0 错误、eslint 0 错误、combat+enemy 测试 1631 项全通过（53 文件）

---

## P3-187：战斗系统测试覆盖盲区（测试·P2）

### 文件级盲区补齐

**forms/store.ts 单测**（`test/combat/forms/store.test.ts`，23 用例）
- 生命周期：initialize（默认/带 savedState）、reset
- 计算属性：currentForm、switchableForms、statModifiers、hpMultiplier/damageMultiplier/defenseMultiplier/speedMultiplier（humanoid/bear/cat/moonkin 四形态）
- 查询：canSwitch（同形态/冷却中/正常）、isSkillAvailable、getSkills、calculateHealAmount
- 操作：switchTo（成功+receiveHeal/同形态拒绝/冷却阻断/receiveHeal 失败不中断）、tickCooldownEnd

**pets/store.ts 单测**（`test/combat/pets/store.test.ts`，51 用例）
- 初始状态：state/currentLevel/currentOwner/activePet/hasActivePet/unlockedPets/allPets 等
- initialize（warlock/hunter）、reset、setLogCallback、updateLevel
- unlockPet/lockPet（含锁定激活宠物时一并解散）
- canSummon（已解锁+资源足够/未解锁/资源不足/已有激活）
- summon（成功/等级同步/资源不足/未解锁/已有激活）
- dismiss、takeDamage（扣血/致死/无激活）、petTakeAction（返回技能/无激活返回 null）
- tickTurn（减少持续时间/到期清空/永续不减少/无激活无副作用）
- getSummonable（按资源过滤/不足返回空/过滤未解锁/猎人分支）
- 完整战斗流程（召唤→行动→受伤→解散）

### 边界场景补齐

**maxStacks 叠加上限测试**（`test/combat/effects/container.test.ts`，+6 用例）
- additive 达到默认 maxStacks=5 后不再 push
- additive 自定义 maxStacks=3
- additive 达到上限时刷新最早效果（value/remainingTurns 更新）
- independent 同样受 maxStacks 限制
- maxStacks=0 时不限制
- 未达 maxStacks 时正常 push

**target_hp 完整边界测试**（`test/combat/usePassiveSkills.test.ts`，+5 用例）
- 目标 hp=0 时比值为 0，满足 `< 0.2`
- 目标满血时比值为 1.0，不满足
- 精确等于阈值 0.2 时 `<` 不满足
- 精确等于阈值 0.2 时 `<=` 满足
- 目标 maxHp=0 时返回 false（除零防护）

### 测试质量改进

**替换随机 ID 生成**（9 个文件）
- `handler.test.ts`、`pipeline.test.ts`、7 个 handler 子目录测试文件
- `Math.random().toString(36).slice(2,6)` → 模块级自增计数器 `++effIdCounter`

### 不在本次范围

- 真实回合制 e2e、Boss 战 e2e、战斗失败/逃跑 e2e（用户指定暂不考虑）
- 职业平衡性基准测试
- effect-type.ts 纯类型模块（无运行时代码可测）

---

## 验证结果

| 检查项 | 结果 |
|--------|------|
| `tsc --noEmit --skipLibCheck` | 0 错误 |
| ESLint（新增/修改测试文件） | 0 错误 |
| combat + enemy 测试 | 1631/1631 通过（53 文件，此前 1546，新增 85 用例） |
