# P3 问题修复归档

> 修复时间：2026-07-29
> 修复批次：P3 优化项（代码质量/一致性）
> 测试状态：145 文件 / 3906 测试通过
> 涉及问题：43 项 P3 已修复，16 项延后/保留

---

## 已修复问题

### P3-82. usePlayerAction 中 passive 参数被显式忽略（死代码）
- **涉及文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L43-L47)
- **修复方案**：保留 passive 参数作为预留扩展点（未来玩家攻击触发 onAttack/onKill 被动钩子），更新注释说明保留原因，避免未来扩展时需修改函数签名和所有调用方。

### P3-83. usePassiveSkills 中多余的解构赋值
- **涉及文件**：[usePassiveSkills.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePassiveSkills.ts#L34-L36)
- **修复方案**：将 `{ addCombatLog, resourceSystems } = { addCombatLog: log.addCombatLog, resourceSystems: state.resourceSystems }` 简化为直接解构 `const { addCombatLog } = log; const { resourceSystems } = state;`。

### P3-84. effects/handler.ts register 方法覆盖已注册处理器时只 warn 不抛异常
- **涉及文件**：[effects/handler.ts](file:///d:/openSource/wow_dnd/src/modules/combat/effects/handler.ts#L73-L77)
- **修复方案**：register 方法新增 `force: boolean = false` 参数。`force=false` 且已注册时：开发环境抛出错误，生产环境保持 console.warn；`force=true` 时静默覆盖。

### P3-85. useEnemyAction 中策略实例每次创建，未共享
- **涉及文件**：[useEnemyAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useEnemyAction.ts#L50-L55)
- **修复方案**：将 strategyRegistry 提取为模块级常量（4 种策略实例无闭包依赖），避免每次调用 useEnemyAction 重建。

### P3-86. pets/service.ts generatePetInstanceId 使用 Math.random
- **涉及文件**：[pets/service.ts](file:///d:/openSource/wow_dnd/src/modules/combat/pets/service.ts#L27-L32)
- **修复方案**：复用统一的 `generateId` 工具函数，替代手动拼接 `Date.now() + Math.random()`。

### P3-87. forms/store.ts 中 getCurrentForm 调用创建了多余的临时对象
- **涉及文件**：[forms/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/forms/store.ts#L120-L121)
- **修复方案**：直接调用 `getFormByType(targetForm)`，避免创建 `{ ...formState.value, currentForm: targetForm }` 临时对象。

### P3-88. useCombatLog 中使用 || 而非 ?? 处理空值
- **涉及文件**：[useCombatLog.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useCombatLog.ts#L72-L77)
- **修复方案**：将 `enemy.physicalAttack || 0` 等四处 `||` 改为 `??`，避免 0 值被吞掉。

### P3-89. combat/store.ts COMBAT_END 事件只发送第一个敌人信息
- **涉及文件**：[combat/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/store.ts#L197)、[bus/types.ts](file:///d:/openSource/wow_dnd/src/modules/bus/types.ts)
- **修复方案**：COMBAT_END 事件载荷补充 `enemyCount` 与 `enemyNames` 摘要字段，保留 `enemy` 首敌引用以向后兼容。

### P3-90. useBossMechanics 中 Number(params?.turns) || 1 的 0 值被吞没
- **涉及文件**：[useBossMechanics.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/useBossMechanics.ts#L89-L132)
- **修复方案**：添加注释说明 turns=0 无实际意义（眩晕/沉默/减益 0 回合等于无效），`||` 提供默认值是合理设计。`debuffType` 改用 `??`。

### P3-91. effects/container.ts 中 addEffectToContainer 对 'max' 策略的合并逻辑可能不符合预期
- **涉及文件**：[effects/container.ts](file:///d:/openSource/wow_dnd/src/modules/combat/effects/container.ts#L33-L39)
- **修复方案**：在 'max' 策略分支添加 JSDoc 注释，明确语义："max 策略：新效果值和已有效果值取较大者。仅比较 value 字段，duration 取较大 duration。"

### P3-93. usePlayerAction 中 AOE 技能的 aoeHits 信息不完整
- **涉及文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L525)
- **修复方案**：在 `aoeHits.push(...)` 中添加 `isCrit` 字段，从上下文暴击判定变量获取。

### P3-94. usePlayerAttack 中 playerAttack 的 finalDamage 与日志中的 damage 不一致
- **涉及文件**：[usePlayerAction.ts](file:///d:/openSource/wow_dnd/src/modules/combat/composables/usePlayerAction.ts#L329-L392)
- **修复方案**：战斗日志的 `damage` 字段及 message 文本由 `finalDamage` 改为 `actualDamage`（实际扣血量，已扣 BOSS 护盾/无敌）。

### P3-96. forms/store.ts 中 switchTo 方法缺少冷却检查的响应式反馈
- **涉及文件**：[forms/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/forms/store.ts#L109-L113)
- **修复方案**：将 switchTo 返回类型从 `Promise<boolean>` 改为 `Promise<{ success: boolean; reason?: string }>`，失败时返回原因。

### P3-97. pets/store.ts 中 petTakeAction 返回类型未显式声明
- **涉及文件**：[pets/store.ts](file:///d:/openSource/wow_dnd/src/modules/combat/pets/store.ts#L227)
- **修复方案**：显式声明 `function petTakeAction(): PetSkill | null`。

### P3-98. character/store.ts selectCharacter 直接 mutate listItem 对象
- **涉及文件**：[character/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L203-L204)
- **修复方案**：改为浅拷贝 `const updatedListItem = { ...listItem, lastPlayedTime: Date.now() }` 后再持久化。

### P3-99. skill/store.ts tickCooldowns 直接 mutate 对象而非替换
- **涉及文件**：[skill/store.ts](file:///d:/openSource/wow_dnd/src/modules/skill/store.ts#L772-L782)
- **修复方案**：先构造 `newCooldowns` 对象再整体赋值 `cooldowns.value`，避免直接 mutate。

### P3-101. equipment/store.ts SLOT_CONFIG 与 ALL_EQUIPMENT_SLOTS 分离
- **涉及文件**：[equipment/service.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/service.ts)、[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts)
- **修复方案**：将 SLOT_CONFIG 定义从 store.ts 移到 service.ts（与 ALL_EQUIPMENT_SLOTS 同文件维护），store.ts 改为从 service.ts 导入。

### P3-103. shop/store.ts mergeItems 使用 `!` 非空断言
- **涉及文件**：[shop/store.ts](file:///d:/openSource/wow_dnd/src/modules/shop/store.ts#L124-L128)
- **修复方案**：使用显式守卫 `const shopId = currentShopId.value; if (!shopId) return [...generatedItems];` 替代非空断言。

### P3-104. equipment/store.ts equipItem 日志未包含槽位信息
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L566)
- **修复方案**：日志消息改为 `装备了：${item.name}（${SLOT_CONFIG[slot].name}）`。

### P3-105. inventory/store.ts organizeInventory 中 totalCount 参数被重新赋值
- **涉及文件**：[inventory/store.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/store.ts#L586-L601)
- **修复方案**：使用独立变量 `let remaining = totalCount;`，避免修改 forEach 回调参数。

### P3-107. character/db.ts saveCharacterListItem 使用 spread 合并
- **涉及文件**：[character/db.ts](file:///d:/openSource/wow_dnd/src/modules/character/db.ts#L21-L37)
- **修复方案**：改为显式构造对象，从 existing 逐字段提取需要保留的字段，避免 spread 合并保留脏字段。

### P3-108. equipment/store.ts persist 和 character/store.ts persistCharacter 均无 try-catch
- **涉及文件**：[equipment/store.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/store.ts#L306-L314)、[character/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/store.ts#L102-L106)
- **修复方案**：为两个 persist 函数添加 try-catch，失败时通过 errorReporter 上报不抛异常。

### P3-109. 多处使用 `|| 0` / `|| undefined` 而非 `?? 0` / `?? undefined`
- **涉及文件**：[inventory/db.ts](file:///d:/openSource/wow_dnd/src/modules/inventory/db.ts#L137)、[equipment/db.ts](file:///d:/openSource/wow_dnd/src/modules/equipment/db.ts#L67-L73)、[skill/db.ts](file:///d:/openSource/wow_dnd/src/modules/skill/db.ts#L197)
- **修复方案**：`bonus` 改为 `??`；`consumable`/`template`/`stackable` 保留 `||` 并添加注释说明归一化语义（false 和 undefined 语义一致）；`usableBy` 改为 `??`。

### P3-110. talents/store.ts totalPoints 计算与 service.ts 不一致
- **涉及文件**：[character/talents/store.ts](file:///d:/openSource/wow_dnd/src/modules/character/talents/store.ts#L53-L55)
- **修复方案**：导入 TALENT_POINT_RULES，改为 `Math.floor(currentLevel.value / TALENT_POINT_RULES.pointsPerLevel)`，与 service.ts 保持一致。

### P3-111. base db 的 cast<T> 辅助函数提供虚假类型安全
- **涉及文件**：[base/db.ts](file:///d:/openSource/wow_dnd/src/modules/base/db.ts#L13-L23)
- **修复方案**：更新注释说明此函数仅做单层断言，不提供运行时类型安全保证。如未来需要运行时校验，可引入 zod schema。

### P3-112. log store initialize 截断后不持久化
- **涉及文件**：[log/store.ts](file:///d:/openSource/wow_dnd/src/modules/log/store.ts#L42-L50)
- **修复方案**：截断后用 `truncated` 标记，仅在确实截断时 fire-and-forget 调用 `saveToDb().catch(...)` 持久化。

### P3-117. console.ts goto 命令硬编码大陆 ID
- **涉及文件**：[console.ts](file:///d:/openSource/wow_dnd/src/modules/console.ts#L988-L992)
- **修复方案**：从 `@/data/config_locations` 导入 `CONTINENTS` 常量，`flatMap` 动态遍历所有大陆获取地点，移除硬编码的大陆 ID。

### P3-118. log 命令的 count 参数无 NaN 校验
- **涉及文件**：[console.ts](file:///d:/openSource/wow_dnd/src/modules/console.ts#L1170-L1174)
- **修复方案**：添加 `if (isNaN(count) || count <= 0) return { success: false, message: '数量必须为正整数' };`。

### P3-119. base db 的 getClassesByRace/Faction 全量加载后内存过滤
- **涉及文件**：[base/db.ts](file:///d:/openSource/wow_dnd/src/modules/base/db.ts#L210-L234)
- **修复方案**：添加注释说明采用全量加载后内存过滤的原因（config_classes 表数据量小 < 20 条，全量加载 + JS 过滤性能足够）。

### P3-120. AdminQueryService 中 queryItemTemplate 串行查询性能
- **涉及文件**：[AdminQueryService.ts](file:///d:/openSource/wow_dnd/src/services/AdminQueryService.ts#L73-L89)
- **修复方案**：改用 `Promise.all` 并行查询消耗品与装备表，消耗品优先返回。

### P3-121. ItemTemplateCache 缓存无过期时间
- **涉及文件**：[ItemTemplateCache.ts](file:///d:/openSource/wow_dnd/src/services/ItemTemplateCache.ts)
- **修复方案**：新增 `saveItemTemplate`/`deleteItemTemplate` 代理方法，写入/删除成功后自动调用 `this.invalidate()` 清除缓存。

### P3-123. useResponsiveGrid 中 ResizeObserver 回调缺少节流
- **涉及文件**：[useResponsiveGrid.ts](file:///d:/openSource/wow_dnd/src/composables/useResponsiveGrid.ts#L26-L41)
- **修复方案**：ResizeObserver 回调改用 `requestAnimationFrame` 节流，`onUnmounted` 中清理 `rafId`。

### P3-124. ErrorHandler 中 wrapAsync 返回 undefined 易被调用方忽略
- **涉及文件**：[ErrorHandler.ts](file:///d:/openSource/wow_dnd/src/services/ErrorHandler.ts#L49-L60)
- **修复方案**：在 wrapAsync 的 JSDoc 中添加 `@deprecated` 标记，引导新代码使用 `tryAsync` 的 Result 模式。

### P3-125. CrossModuleQuery 缺少错误处理与降级策略
- **涉及文件**：[CrossModuleQuery.ts](file:///d:/openSource/wow_dnd/src/services/CrossModuleQuery.ts#L25-L50)
- **修复方案**：引入 errorReporter，每个查询方法添加 try-catch，失败时记录错误并降级返回安全默认值（`null` 或 `[]`）。

### P3-127. main.ts 与 App.vue 重复调用 dataInitializer.initializeData
- **涉及文件**：[App.vue](file:///d:/openSource/wow_dnd/src/App.vue#L143)、[main.ts](file:///d:/openSource/wow_dnd/src/main.ts#L86)
- **修复方案**：在 App.vue 中移除对 dataInitializer.initializeData 的重复调用，main.ts 中将 initializeData() 移至 app.mount() 之前完成。

### P3-129. CharacterLifecycleService 技能栏填充依赖数组顺序
- **涉及文件**：[CharacterLifecycleService.ts](file:///d:/openSource/wow_dnd/src/services/CharacterLifecycleService.ts#L65-L69)
- **修复方案**：填充技能栏前对 skills 按 `unlockLevel` 升序、`id` 升序稳定排序，再取前 4 个。

### P3-131. calculateAllAttributes 返回类型未显式声明
- **涉及文件**：[calculations.ts](file:///d:/openSource/wow_dnd/src/utils/calculations.ts#L146-L170)
- **修复方案**：定义 `Attributes` 接口并显式声明 `calculateAllAttributes(stats: Stats): Attributes`。

### P3-132. CAMP_HEAL_HP 使用魔法数字 9999
- **涉及文件**：[config/exploration.ts](file:///d:/openSource/wow_dnd/src/config/exploration.ts#L57-L59)
- **修复方案**：将 `9999` 改为 `Number.MAX_SAFE_INTEGER`，表示"完全恢复"，由角色上限自然裁剪。

### P3-133. config/exploration.ts 概率常量分散定义，缺少聚合校验
- **涉及文件**：[config/exploration.ts](file:///d:/openSource/wow_dnd/src/config/exploration.ts#L54-L68)
- **修复方案**：添加开发期聚合校验，检查五项概率基础值之和是否等于归一化基数（100），不等时输出 console.warn。
- **额外修复**：校验暴露了真实的配置错误——原五项基础值之和为 102（20+25+12+15+30），将 `EMPTY_PROBABILITY_BASE` 从 30 调整为 28，使基础值之和恰好等于归一化基数 100，消除校验警告并修正概率分布意图。

### P3-134. config_shops.ts 缺少商店与地点的关联
- **涉及文件**：[shop/types.ts](file:///d:/openSource/wow_dnd/src/modules/shop/types.ts)
- **修复方案**：在 ShopConfig 接口中新增可选字段 `locationId?: string`（仅类型定义，未填充现有数据）。

### P3-140. config/log.ts 注释引用已废弃任务编号
- **涉及文件**：[config/log.ts](file:///d:/openSource/wow_dnd/src/config/log.ts)
- **修复方案**：将注释中的任务编号（PERF-3、BIZ-12）替换为具体的业务描述。

---

## 延后/保留的问题（16 项）

### P3-92. useCombatState 中 bossPhaseManagers 使用普通 Map 而非响应式 — 延后
- **原因**：改为响应式可能影响现有测试和 UI 渲染，需评估是否有 UI 需要显示 Boss 当前阶段。

### P3-95. useEnemyAction 中敌人普通攻击默认物理伤害 — 需游戏设计确认
- **原因**：是否支持魔法普攻取决于游戏设计，需产品确认。

### P3-100. character/store.ts setRace / setClass 不校验阵营/种族兼容性 — 需谨慎评估
- **原因**：可能影响角色创建流程，需评估所有创建路径。

### P3-102. skill/store.ts equipSkill 允许装备未学习技能 — 需谨慎评估
- **原因**：需确认"仅已学技能可装备"还是"装备即学习"的设计意图。

### P3-106. item-template/cache.ts load 失败后不缓存错误 — 需谨慎实现
- **原因**：退避策略需要仔细设计，避免过度复杂。

### P3-113. calculateChecksum 使用弱哈希算法 — 延后
- **原因**：SHA-256 迁移需要评估性能影响和兼容性。

### P3-115. modules/index.ts 中 boss/enemy 类型名碰撞 — 影响面广
- **原因**：类型重命名涉及多个模块的导入更新。

### P3-116. console.ts 通过 window.__gameState 切换游戏状态 — 大型重构
- **原因**：GameState 迁移到 Pinia store 是大型重构。

### P3-122. useSkillDisplay 类型定义不严格 — 中等风险
- **原因**：需要从 Skill 类型提取联合类型，可能影响多个组件。

### P3-126. App.vue 中 Toast 组件 props 传递方式不规范 — 可能影响 UI
- **原因**：UI 组件重构需要视觉验证。

### P3-128. GameBootstrap 串行初始化可能影响启动速度 — 需分析依赖
- **原因**：并行化需要仔细分析 Store 间的依赖关系。

### P3-130. LEVEL_EXP_REQUIREMENTS 类型不够严格 — 中等风险
- **原因**：mapped type 可能影响配置文件的使用方式。

### P3-135. config_class_items.ts 只覆盖 6 个职业 — 内容创作
- **原因**：补充 7 个职业的专属装备是内容创作工作，非代码修复。

### P3-136. config_quests.ts 任务类型单一 — 内容创作
- **原因**：补充 collect、talk、explore 类型任务是内容创作工作。

### P3-137. config_mobs.ts 中怪物 ID 与种族 ID 同名 — 影响面广
- **原因**：ID 重命名涉及配置数据、DB 数据和引用代码的全面更新。

### P3-138. BaseDbService toStorage/toRuntime 默认实现使用 unknown 断言 — 保留默认实现
- **原因**：测试中的 DefaultTestService 依赖默认实现，改为 abstract 会破坏测试。保留默认实现并添加注释说明。

### P3-139. config_class_talents.ts 中 stat_bonus 的 stat 字段类型不一致 — 中等风险
- **原因**：拆分类型需要更新所有引用 stat_bonus 的配置和消费代码。

---

## 修复统计

| 类别 | 数量 | 说明 |
|------|------|------|
| 已修复 | 43 | P3-82 ~ P3-140 中的 43 项 |
| 延后/保留 | 16 | 需谨慎评估、大型重构或内容创作 |
| **合计** | **59** | |
