# 第六轮修复归档 (2026-08-10 13:40:00)

> 第六轮全量代码审查，7 子代理覆盖 303 源文件，发现 109 项问题。
> 本次修复 27 项（P1×7 P2×20），验证排除 1 项。
> tsc/eslint 零错误，vitest 5775 项通过（51 项预存组件测试失败）。

---

## P1 — 阻断（7 项，全部修复）

### P9-001: ResourceSystemFactory 缓存实例跨战斗累积 maxValue
- **文件**: src/modules/combat/resources/BaseResourceSystem.ts
- **修复**: 新增 `baseMaxValue` 字段记录构造时上限，`reset()` 中同时重置 `_maxValue` 到 `baseMaxValue`，防止 `addMaxBonus` 跨战斗无限累积。

### P9-002: AOE 技能击杀 Boss 时不检查复活机制
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: AOE 循环中捕获 `takeDamage` 返回值，对死亡 Boss 调用 `boss.checkBossRevive(e)`。

### P9-003: 宠物攻击击杀 Boss 时不检查复活机制
- **文件**: src/modules/combat/composables/usePetAction.ts
- **修复**: 捕获 `takeDamage` 返回值，若 Boss 被击杀则调用 `checkBossRevive`。

### P9-004: 宠物联动伤害绕过 Boss 防御/反击机制
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: 宠物伤害经过 `boss.applyBossDefenseMechanics` 和 `boss.applyBossCounterMechanics`。

### P9-005: playerSkill 未匹配分支时不结束回合
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: 函数末尾 return 前加兜底 `initiative.endPlayerTurn()`，防止玩家回合卡死。

### P9-006: 角色加载时套装 bonus 与天赋 stat_bonus 被重复施加
- **文件**: src/modules/equipment/composables/useSetBonus.ts, src/modules/equipment/store.ts, src/modules/character/talents/store.ts
- **修复**: equipment initialize 改用 `syncAppliedBonuses()` 对齐 diff 基线（不调用 applyBonus/removeBonus）；talent initialize 改为 `lastAppliedStats = { ...statBonuses.value }` 跳过 apply。

### P9-007: 切换角色时 talent/equipment store 的 diff 基线未重置
- **文件**: src/modules/character/talents/store.ts
- **修复**: talent initialize 中先 `lastAppliedStats = {}` 再赋值当前 statBonuses，消除旧角色基线残留。（配合 P9-006 的 syncAppliedBonuses 共同修复。）

---

## P2 — 重要（20 项修复 + 1 项排除）

### P9-008: DOT 击杀 Boss 时不检查复活机制
- **文件**: src/modules/combat/composables/useInitiative.ts
- **修复**: `tickAllEffects` 中 DOT 伤害后遍历被击杀的 Boss 调用 `boss.checkBossRevive`。

### P9-009: FocusSystem 缺少 onDamaged 钩子实现
- **文件**: src/modules/combat/resources/FocusSystem.ts
- **修复**: 添加 `onDamaged(amount)` 方法，调用 `generate(Math.floor(amount * 0.1), 'damaged')`。

### P9-011: summon_pet 技能硬编码 focus 系统查找，术士无法召唤
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: 根据 `pet.petStore.currentOwner` 查找对应资源类型（warlock→soul_shard, hunter→focus）。

### P9-012: 日志持久化截断方向错误——保存最旧条目而非最新
- **文件**: src/modules/log/db.ts
- **修复**: `logs.slice(0, MAX_LOG_ENTRIES)` 保留数组头部（最新）条目；从 config/log.ts 导入统一的 `MAX_LOG_ENTRIES`（1000）替代硬编码 500。

### P9-013: 物品堆叠上限硬编码 MAX_STACK=10，忽略 per-type maxStack
- **文件**: src/modules/inventory/composables/useInventoryItems.ts
- **修复**: addItem 中使用 `itemTemplate.maxStack ?? MAX_STACK` 替代硬编码。

### P9-016: Admin CRUD 操作后未失效物品模板缓存
- **文件**: src/modules/admin/store.ts
- **修复**: saveRecord/deleteRecord 成功后判断表名，对 config_equipment_items/config_set_definitions 调用 `configCache.invalidate()`。

### P9-019: ConfigCache.invalidate 与进行中的 load 产生竞态
- **文件**: src/modules/config/cache.ts
- **修复**: invalidate 中同时清除对应的 `_xxxLoadingPromise = null`。

### P9-020: cascadeDeleteCharacter 未清理战斗日志 → 验证排除
- **文件**: src/services/CharacterLifecycleService.ts
- **结论**: `runtime_combatLogs` 表仅按 `combatId, timestamp` 索引，无 `characterId` 字段。战斗日志属于战斗会话而非角色，无法高效按角色删除。非问题。

### P9-021: 导入备份文件无大小限制，存在 DoS 风险
- **文件**: src/modules/data/importer.ts
- **修复**: validateBackup 开头检查 `file.size > 50MB` 时返回错误。

### P9-022: runMigrations 未处理版本降级场景
- **文件**: src/modules/data/migrations/index.ts
- **修复**: `fromVersion > CURRENT_DATA_VERSION` 时抛出错误提示用户更新应用。

### P9-024: 升级后天赋等级不同步，玩家无法使用新天赋点
- **文件**: src/composables/useGameActions.ts
- **修复**: `onLevelUp` 中调用 `talentStore.updateLevel(payload.newLevel)`。

### P9-025: 升级后新技能不会自动学习
- **文件**: src/composables/useGameActions.ts
- **修复**: `onLevelUp` 中调用 `void skillStore.checkLevelUnlocks(true)`。

### P9-026: 学习非 stat_bonus 天赋时不持久化
- **文件**: src/modules/character/talents/store.ts, src/modules/character/store.ts
- **修复**: `syncAllocationsToCharacter` 末尾增加 `void characterStore.persistCharacter()`；character store 导出 `persistCharacter`。

### P9-027: applyStatBonusesToCharacter 的 fire-and-forget IIFE 存在异步竞态
- **文件**: src/modules/character/talents/store.ts
- **修复**: 移除 IIFE，改为同步执行 `applyBonus`/`removeBonus`（Pinia action 同步部分立即更新 bonusStats），await 完成后同步更新 `lastAppliedStats`。

### P9-028: useEquipmentState.initialize 缺少 try/finally，异常时 isLoading 卡死
- **文件**: src/modules/equipment/composables/useEquipmentState.ts
- **修复**: 用 `try { ... } finally { isLoading.value = false; }` 包裹。

### P9-031: Boss phases 浅拷贝导致 mechanic 状态跨战斗污染
- **文件**: src/modules/boss/service.ts
- **修复**: 深拷贝 phases 及 mechanics 数组。

### P9-032: executeBossMechanic lastTriggerTurn falsy 检查导致回合 0 机制失效
- **文件**: src/modules/boss/engine.ts
- **修复**: `!== undefined` 替代 falsy 判断。

### P9-037: CharacterSelectPopup 重复调用 selectCharacter
- **文件**: src/components/popup/CharacterSelectPopup.vue
- **修复**: 移除 popup 中的 `selectCharacter` 调用，仅 emit 'select' 由 App.vue 统一处理。

### P9-038: Tag 组件 customStyle 非响应式
- **文件**: src/components/common/Tag.vue
- **修复**: 改为 `computed(() => ({ '--tag-color': props.color || 'inherit' }))`。

### P9-044: BossPhaseStrategy 狂暴阶段冗余分支 + 硬编码 0.3
- **文件**: src/modules/combat/ai/strategies.ts
- **修复**: 合并冗余分支，使用 `PLAYER_LOW_HP_THRESHOLD`（0.25）替代硬编码 0.3，残血时 50% 概率使用技能。
