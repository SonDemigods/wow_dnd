# 第九轮全量代码审查修复记录

> 日期：2026-08-12
> 审查范围：8 子代理覆盖 323 源文件
> 发现问题：54 项（P1×1 P2×30 P3×23）
> 修复：46 项（P1×1 P2×26 P3×19）
> 验证排除：4 项（P11-200 设计预留字段、P11-201 设计约定 boardId=location.id、P11-216 P3 DB-12 审计决策、P11-204 已修复）
> 待修复 P3：4 项（P11-005/106/108/505，影响低，暂不修复）
> tsc/eslint：零错误
> vitest：5774 项通过，53 项预存组件测试失败，无新回归

---

## P1 修复

### P11-301: sellItem 的 gainGold 未包裹 try-catch — 物品丢失
- **文件**: src/modules/shop/store.ts
- **修复**: 将 `gainGold` 包裹进 try-catch，失败时回滚背包（addItem + flushPersist），显示 toast 提示。

---

## P2 修复

### P11-001: usePlayerSkill 中 endPlayerTurn() 被重复调用
- **文件**: src/modules/combat/composables/usePlayerSkill.ts
- **修复**: 在 5 个分支（单体伤害/buff/debuff×2/heal）的 endPlayerTurn() 后添加 return 语句。

### P11-002: usePlayerItem 缺失 Boss 复活检查（多敌场景）
- **文件**: src/modules/combat/composables/usePlayerItem.ts
- **修复**: 将 Boss 复活检查顺序改为与 playerAttack 一致（先 checkBossRevive，再 aliveEnemies.length===0）。

### P11-003: DOT 击杀 Boss 复活时仍触发 onKill
- **文件**: src/modules/combat/composables/useInitiative.ts
- **修复**: 将 onKill 调用改为 `if (!boss.checkBossRevive(enemy))` 条件执行。

### P11-004: healing_zone 机制 NaN 风险
- **文件**: src/modules/combat/composables/useBossMechanics.ts
- **修复**: 添加 `Number.isFinite(Number(rawHeal))` 防御。

### P11-100: setRace 缺少职业-种族兼容性校验
- **文件**: src/modules/character/store.ts
- **修复**: 在 setRace 中增加 isClassRaceCompatible 校验。

### P11-101: handleDeath 持久化死亡状态后复活，崩溃可致角色永久卡死
- **文件**: src/modules/character/store.ts
- **修复**: 移除 handleDeath 中复活前的 persistCharacter() 调用。

### P11-102: reapplySetBonuses 错误恢复设置错误基线
- **文件**: src/modules/equipment/composables/useSetBonus.ts
- **修复**: 移除 try-catch（catch 块设置错误基线），让异常上抛保持旧值不变。

### P11-103: equipItem 中 reapplySetBonuses 失败回滚未重算套装 bonus
- **文件**: src/modules/equipment/composables/useEquipmentOps.ts
- **修复**: 在 equipItem catch 回滚中增加 `await setBonus.reapplySetBonuses()`。

### P11-104: canEquipItem 错误拒绝双手武器
- **文件**: src/modules/equipment/service.ts
- **修复**: 移除 `two_handed && equipment.weapon2 → false` 旧逻辑。

### P11-105: selectCharacter 状态设置顺序不一致
- **文件**: src/modules/character/store.ts
- **修复**: 将 character.value 等赋值移到 await setCurrentCharacterId 之前。

### P11-204: 多选项事件格在玩家走开后可重复触发
- **文件**: src/modules/exploration/store.ts
- **修复**: 未完成的事件格在 pendingEventCell 已设置时拒绝重新交互。

### P11-300: organizeInventory 使用硬编码 MAX_STACK
- **文件**: src/modules/inventory/composables/useInventoryItems.ts
- **修复**: 改为 `itemInfo.maxStack ?? MAX_STACK`。

### P11-302: saveItemTemplate 丢失多效果与 percentValue
- **文件**: src/modules/inventory/db.ts, src/modules/inventory/types.ts
- **修复**: 持久化完整 effects 数组（含 percentValue），mapToItem 优先读取 modern 格式。

### P11-303: saveItemTemplate/mapToItem 不持久化 maxStack
- **文件**: src/modules/inventory/db.ts, src/modules/inventory/types.ts
- **修复**: 添加 maxStack 字段到 ItemDataStorage/ItemStorage，saveItemTemplate 写入，mapToItem 读取。

### P11-305: buyItem 回购路径回滚遗漏 soldItems
- **文件**: src/modules/shop/store.ts
- **修复**: 在 buyItem catch 块中添加 _replaceSoldItems 回滚。

### P11-401: validateItemSetReferences 未校验 SET_PARTS
- **文件**: src/data/validate.ts
- **修复**: 增加 SET_PARTS 遍历校验。

### P11-402: 缺少 Boss skillPool 引用校验
- **文件**: src/data/validate.ts
- **修复**: 新增 validateBossSkillPool() 函数。

### P11-403: AREA_EVENT_TEMPLATES 的 key 与 location.id 不匹配
- **文件**: src/data/config_area_events.ts, src/modules/exploration/store.ts
- **修复**: 新增 THEME_TO_EVENT_CATEGORY 映射 + getAreaEventTemplates(locationId) 辅助函数。

### P11-404: 唤魔者缺少 8 级技能
- **文件**: src/data/config_skills.ts
- **修复**: 新增 evoker_firestorm（烈焰风暴）AOE 魔法伤害技能。

### P11-405: RARITY_LEVEL_MAP 缺少 legendary
- **文件**: src/config/exploration.ts
- **修复**: 添加 `legendary: 9`。

### P11-406: boss_lich 缺少 attackType: 'magical'
- **文件**: src/data/config_bosses.ts
- **修复**: 添加 `attackType: 'magical'`。

### P11-408: boss_death_lord 在 winterspring 难度过高
- **文件**: src/data/config_locations.ts
- **修复**: 替换为 boss_frost_wyrm（maxHp 200，适合 Lv14-18）。

### P11-500: MigrationService 版本降级被静默忽略
- **文件**: src/modules/data/migrations/service.ts
- **修复**: 拆分 `>=` 为 `===` 和 `>`，降级返回失败。

### P11-501: ConfigCache 在 admin 编辑 class_talents/class_passives 后未失效
- **文件**: src/modules/admin/store.ts
- **修复**: 按表名精准失效：class_talents→invalidate('talents'), class_passives→invalidate('passives'), config_set_definitions→invalidate('sets')。

### P11-502: initializeData 每次启动覆盖 config_locations
- **文件**: src/modules/data/initializer.ts
- **修复**: 将 initLocations()/initContinents() 移入 if (!isInitialized) 块。

### P11-503: level 控制台命令降级时调用 destructive reset()
- **文件**: src/modules/console/commands/character.ts
- **修复**: 降级时仅调整 level/exp/expToNextLevel + persistCharacter，不调用 reset()。

### P11-600: ExplorationView 鼠标拖拽平移卡顿
- **文件**: src/components/ExplorationView.vue
- **修复**: 阈值仅用于判定 drag vs click，hasDragged 后持续更新 pan。

### P11-601: InventoryPopup 竞态条件
- **文件**: src/components/popup/InventoryPopup.vue
- **修复**: 添加 await 到 organizeInventory/removeItemByIndex。

---

## P3 修复

### P11-006: burn 倍率 1.5 硬编码 → config 常量 BURN_DAMAGE_MULTIPLIER
### P11-007: PLAYER_LOW_HP_THRESHOLD 提取到 config
### P11-107: createCharacter 添加角色名称非空校验
### P11-304: canStackItem 改用 item.maxStack ?? MAX_STACK
### P11-306: dropItemsByIndices 日志添加 await
### P11-307: shop buyItem/sellItem 日志添加 await
### P11-308: lastRefresh Map 改用 _replaceLastRefresh 整体替换
### P11-309: useItem 改为 `value > 0 || (percentValue && percentValue > 0)`
### P11-310: initialize 添加 try-finally 保护 isLoading
### P11-400: ENEMY_AOE_DAMAGE_MULTIPLIER 注释更新 0.7→0.8
### P11-407: config_class_passives.ts 注释 3→6
### P11-409: validateSkillBuffs 添加 'thorn' + EffectType 联合类型
### P11-504: AdminDbService.update 包裹 db.transaction
### P11-506: gold 命令添加 amount<=0 校验 + requireCharacter；hp/mp 添加 requireCharacter
### P11-602: InventoryPopup doEquip 添加 try/finally
### P11-603: SkillsPanel selectSkill 清除 selectedSlotIndex
### P11-604: SkillsPanel findEmptySlot 统一为 slot === null
### P11-605: CombatPopup 改用 info.kind !== 'consumable'
### P11-700: useCharacterCreation loadData 添加 try/catch
### P11-701: useGameActions handleShopClose 添加 try/catch
### console/framework.ts rarityColorKey 修复 common 被注释的预存 bug

---

## 验证排除项

| 编号 | 说明 |
|------|------|
| P11-200 | unlockedZones/completedZones 为 P4-020 设计预留字段，实际解锁由等级检查决定。 |
| P11-201 | getQuestRequiredMonsters 的 boardId=location.id 为 P6-105 确认的设计约定。 |
| P11-216 | 任务奖励物品添加失败仍标记完成是 P3 DB-12 审计决策，跨模块事务成本高，已有 toast 提示。 |

---

## 待修复 P3（影响低，暂不修复）

| 编号 | 说明 |
|------|------|
| P11-005 | usePlayerItem 伤害在物品消耗前施加，需较大重构调换顺序。 |
| P11-106 | castSkill fallback 到 skillTemplates，调用方已保证不施放未学习技能。 |
| P11-108 | 天赋初始化未校验存档分配合法性，影响仅限旧存档配置变更场景。 |
| P11-505 | ConfigCache invalidate 竞态条件，单机游戏触发概率极低。 |
